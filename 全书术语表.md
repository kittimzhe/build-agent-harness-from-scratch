# 全书术语表

本术语表收录教程中出现的关键术语，按主题分组，便于速查。每条尽量给一句工程化解释，而非字典定义。

---

## 一、LLM 与基础

| 术语 | 一句话解释 |
| --- | --- |
| LLM | 大语言模型，基于 Transformer 的自回归生成模型 |
| Token | 模型处理的最小单位，1 个汉字约 1–2 token |
| 上下文窗口 | 模型一次能「看到」的最大 token 数 |
| KV Cache | 推理时缓存的 Key/Value，避免重复计算前缀 |
| Temperature / Top-p | 采样参数，控制生成的随机性与多样性 |
| 幻觉 | 模型生成看似合理但与事实不符的内容 |
| 对齐 | 让模型行为符合人类意图与价值观（RLHF/DPO 等） |

## 二、Prompt 与 Context

| 术语 | 一句话解释 |
| --- | --- |
| Prompt Engineering | 怎么写出有效指令 |
| Context Engineering | 多轮中持续组织「最有用的那部分 tokens」进上下文 |
| Few-shot | 在 prompt 中给出少量示例引导输出 |
| Function Calling | 让模型按 schema 输出函数调用参数。**首次出现**：[第 05 章](05-手写第一个工具循环.md#1、从聊天到行动缺的那一环) |
| 结构化输出 | 让模型输出符合 JSON Schema 的可解析数据。**首次出现**：[第 07 章](07-结构化输出与Schema设计.md#1、为什么模型输出需要结构) |

## 三、Agent 与 Harness

| 术语 | 一句话解释 |
| --- | --- |
| Agent | LLM 在循环中自主调用工具、观察、决策的系统 |
| Harness | 包裹 LLM 的脚手架/运行时：状态、工具、trace、权限等。**首次出现**：[第 01 章](01-LLM调用与环境准备.md#1、为什么这份教程不用现成框架) |
| ReAct | Thought→Action→Observation 循环。**首次出现**：[第 05 章](05-手写第一个工具循环.md#4、终止条件与护栏循环的方向盘)（定位：循环是本体，ReAct 是教具） |
| Plan-and-Execute | 先规划再执行的模式 |
| Checkpoint | 任务状态的持久化快照，用于恢复 |
| Trace | 一次 Agent 执行的完整调用链记录 |
| Memory | 短期/长期/向量记忆，跨轮或跨会话 |

## 三·五、本教程内核组件（随章节生长）

| 术语 | 一句话解释 |
| --- | --- |
| `LLMClient` | 调用层：统一 4 提供商的 chat/stream，返回 `LLMResult`。**首次出现**：[第 01 章](01-LLM调用与环境准备.md#5、封装-llmclient第一块-harness-砖) |
| `ChatSession` | 状态层：攒消息历史、累积 token 用量、提供截断。**首次出现**：[第 02 章](02-消息状态与上下文窗口.md#2、封装-chatsession第二块砖) |
| `Tool` | 把普通 Python 函数包装成带 schema 的模型可调用工具。**首次出现**：[第 05 章](05-手写第一个工具循环.md#3、手写-agentloop第三块砖) |
| `AgentLoop` | 循环层：执行「要工具→执行→回喂」直到终止，带 max_rounds 护栏。**首次出现**：[第 05 章](05-手写第一个工具循环.md#3、手写-agentloop第三块砖) |
| `tool` 角色 | 消息列表第四种角色，承载工具执行结果，靠 `tool_call_id` 对应请求。**首次出现**：[第 05 章](05-手写第一个工具循环.md#1、从聊天到行动缺的那一环) |
| 护栏 / max_rounds | 循环的最大轮数预算，防止模型「不停要工具」烧穿账单。**首次出现**：[第 05 章](05-手写第一个工具循环.md#4、终止条件与护栏循环的方向盘) |
| `stopped_by` | `run()` 返回字段，区分自然终止（model）与预算终止（max_rounds）。**首次出现**：[第 05 章](05-手写第一个工具循环.md#31-设计要点) |
| `ToolError` | 工具执行/容错层的统一错误，可回喂给模型。**首次出现**：[第 06 章](06-工具集合与容错.md#2、三件套重试超时幂等) |
| `RetryPolicy` | 重试 / 超时 / 幂等的配置：指数退避、超时预算、幂等门槛。**首次出现**：[第 06 章](06-工具集合与容错.md#3、内核harness-toolspy第四块砖) |
| `ResilientTool` | 有容错策略的 Tool，与 Tool 同接口，AgentLoop 无感调用。**首次出现**：[第 06 章](06-工具集合与容错.md#3、内核harness-toolspy第四块砖) |
| `ToolRegistry` | 工具注册表：集中管理工具集合 + 各自策略 + 幂等键去重。**首次出现**：[第 06 章](06-工具集合与容错.md#3、内核harness-toolspy第四块砖) |
| 幂等 / 幂等键 | 重试安全的前提：同操作多次执行结果不变；非幂等靠 idempotency_key 去重。**首次出现**：[第 06 章](06-工具集合与容错.md#23-幂等idempotency) |
| `StructuredOutputError` | 结构化输出的解析/校验失败，message 可回喂模型自纠。**首次出现**：[第 07 章](07-结构化输出与Schema设计.md#3、内核harness-schemapy第五块砖) |
| `extract_json` | 从模型输出（带围栏/前后废话）里稳健抠出 JSON。**首次出现**：[第 07 章](07-结构化输出与Schema设计.md#31-extract-json抠出-json) |
| `strict_validate` | JSON → Pydantic 模型，做类型/必填/约束校验。**首次出现**：[第 07 章](07-结构化输出与Schema设计.md#32-strict-validate解析-校验一条龙) |
| `structured_chat` | 字符串之外的结构化调用：JSON Mode + Pydantic + 自纠重试。**首次出现**：[第 07 章](07-结构化输出与Schema设计.md#33-structured-chat签名外的包装) |
| 自纠重试 | 解析/校验失败时把错误回喂模型让它自己修（输出侧，对应 06 的工具侧回喂）。**首次出现**：[第 07 章](07-结构化输出与Schema设计.md#23-自纠重试第三层保证最终可用) |
| `estimate_tokens` | 粗略估算 token 数（中 1 字≈1 token，其余 4 字符≈1 token）。**首次出现**：[第 08 章](08-Context治理.md#31-estimate-tokens先学会量) |
| `keep_last_turns` | 按轮成对裁：保留 system + 最近 N 个完整回合，根治「历史以 assistant 开头」。**首次出现**：[第 08 章](08-Context治理.md#32-keep-last-turns先修好那把刀) |
| `summarize` / `compact` | 用 LLM 把旧历史压成摘要；compact = 摘要 + 最近 N 轮原文。**首次出现**：[第 08 章](08-Context治理.md#33-summarize--compact压缩) |
| `select_by_budget` | 按 token 预算取舍：从最新往旧塞直到预算满。**首次出现**：[第 08 章](08-Context治理.md#34-select-by-budget取舍) |
| `ReferenceLibrary` | 按需读取最小实现：上下文放「目录」，需要时再拉全文（RAG 雏形）。**首次出现**：[第 08 章](08-Context治理.md#35-referencelibrary按需读取) |
| lost in the middle | 长上下文里模型对开头结尾记得牢、对中段利用率断崖下降。**首次出现**：[第 08 章](08-Context治理.md#1、回到第-02-章那三根刺) |
| 短期记忆 / 长期记忆 / 向量记忆 | 记忆三层：会话内（messages）/ 跨会话精确 key（文件）/ 按语义相似检索（向量）。**首次出现**：[第 09 章](09-记忆体系.md#1、记忆的三层短期--长期--向量) |
| `FileMemory` | 长期记忆：key→value 事实库，`save()` 落盘才算跨会话。**首次出现**：[第 09 章](09-记忆体系.md#41-filememory跨会话的精确记忆) |
| `VectorMemory` | 语义记忆：编码→余弦相似→top-k 检索，embed 可换成真 embedding。**首次出现**：[第 09 章](09-记忆体系.md#42-vectormemory语义检索的机械结构) |
| `toy_embed` / `cosine` | 玩具嵌入（字符 2-gram 词袋）与余弦相似度——向量检索的教具骨架。**首次出现**：[第 09 章](09-记忆体系.md#42-vectormemory语义检索的机械结构) |
| `retrieved_context` | RAG 最小闭环：检索命中的记忆片段拼进 prompt。**首次出现**：[第 09 章](09-记忆体系.md#43-retrieved-contextrag-的最小闭环) |
| RAG vs Memory | 读 vs 写：RAG 按 query 现查外部知识（读），Memory 对话中持续写本体状态（写）。**首次出现**：[第 09 章](09-记忆体系.md#3、rag-与-memory-的边界那道老题) |
| ReAct | 边想边做：think→act→observe 循环，每轮只决定下一步，无全局计划。**首次出现**：[第 05 章](05-手写第一个工具循环.md#1、一个最小工具循环) |
| Plan-and-Execute | 先列计划再执行：`make_plan` 出步骤清单 → `execute_plan` 逐步打钩。**首次出现**：[第 10 章](10-任务拆解与Planning.md#1、两种范式react-与-plan-and-execute) |
| `Plan` / `PlanStep` | 计划的一等对象：步骤清单 + 状态机（pending→in_progress→done/failed）+ 可观察进度。**首次出现**：[第 10 章](10-任务拆解与Planning.md#31-plan--planstep计划状态机) |
| `make_plan` | 用结构化输出（复用第 07 章）让 LLM 吐 `{"steps":[...]}` 步骤清单。**首次出现**：[第 10 章](10-任务拆解与Planning.md#32-make-plan复用自己的第-07-章) |
| `execute_plan` | 逐步执行计划，每步置状态；失败即停（第 11 章再谈补偿）。**首次出现**：[第 10 章](10-任务拆解与Planning.md#33-execute-plan逐步执行--失败即停) |
| Self-Reflection | 反思：把失败信息回喂给模型，让它判「继续/放弃」并给出下一步。**首次出现**：[第 11 章](11-失败策略与反思.md#2、self-reflection-的本质把失败喂回去) |
| 傻重试 vs 聪明重试 | 06 同动作退避重来（挡瞬时故障）vs 11 反思换思路（挡思路错）。**首次出现**：[第 11 章](11-失败策略与反思.md#1、失败策略不单独成范式它服务谁) |
| `reflect` | 一次反思：失败日志 + 目标 → `{verdict, reason, next_action}`（复用 structured_chat）。**首次出现**：[第 11 章](11-失败策略与反思.md#31-reflect一次反思) |
| `retry_with_reflection` | 反思式重试循环：失败→反思→换思路→再试，超限或 give_up 即停。**首次出现**：[第 11 章](11-失败策略与反思.md#32-retry-with-reflection反思式重试循环) |
| Checkpoint | 状态快照落盘：把运行中的状态序列化成可恢复的文件。**首次出现**：[第 12 章](12-Checkpoint与状态恢复.md#1、为什么状态要能落地) |
| 状态序列化 | 对象 ↔ 可 JSON 的 dict（`plan_to_dict` / `plan_from_dict`）。**首次出现**：[第 12 章](12-Checkpoint与状态恢复.md#31-序列化往返) |
| 断点续跑 | 进程崩了从盘上读回状态接着做，只重跑「没 done」的步骤。**首次出现**：[第 12 章](12-Checkpoint与状态恢复.md#2、断点续跑-vs-从头再跑) |
| `save_checkpoint` / `load_checkpoint` | 计划状态落盘与恢复。**首次出现**：[第 12 章](12-Checkpoint与状态恢复.md#3、内核harnessstatepy第十块砖) |
| `run_plan_with_checkpoint` | 每步前后都落盘地执行计划；failed 恢复时重跑（at-least-once）。**首次出现**：[第 12 章](12-Checkpoint与状态恢复.md#32-run-plan-with-checkpoint每步都落盘) |
| Durable Execution | 状态是真源、SAVE 是常态、执行只推进状态并每步落盘。**首次出现**：[第 12 章](12-Checkpoint与状态恢复.md#1、为什么状态要能落地) |
| Agent 状态机 | 显式标出 Agent「现在到哪了」：new→running→done/error，非法迁移报错。**首次出现**：[第 13 章](13-封装MiniAgentRuntime.md#2、agent-状态机显式--有护栏) |
| 事件循环（runtime） | 一次 run = 一圈状态机，内部委托 AgentLoop 和模型过招，关键节点发事件。**首次出现**：[第 13 章](13-封装MiniAgentRuntime.md#31-事件循环--钩子) |
| `MiniAgent` | 可复用的 mini Agent runtime：状态机 + 事件循环 + 可插拔钩子。**首次出现**：[第 13 章](13-封装MiniAgentRuntime.md#1、从十块砖到一个-agent) |
| `RuntimeEvent` / 钩子 | run 过程中发出的 start/finish/error 事件，及观察这些事件的 on_event 回调（trace 的原料）。**首次出现**：[第 13 章](13-封装MiniAgentRuntime.md#3、内核harnessruntimepy第十一块砖) |
| log / metric / trace / replay | 可观测四件套：记录事件 / 聚合指标 / 串成时间线 / 离线重放。**首次出现**：[第 14 章](14-可观测Trace日志回放.md#1、log--metric--trace--replay-四件套) |
| `Tracer` | 可观测收集端：包装 llm 记每次 chat + 挂 MiniAgent 钩子收状态，不侵入 AgentLoop。**首次出现**：[第 14 章](14-可观测Trace日志回放.md#3、内核harnesstracepy第十二块砖) |
| `TraceEvent` / `Trace` | 一条事件（seq+ts+type+payload）与一次 run 的完整记录。**首次出现**：[第 14 章](14-可观测Trace日志回放.md#3、内核harnesstracepy第十二块砖) |
| `ScriptedLLM` | 按脚本依次吐 LLMResult 的假模型：replay 原料 + 离线测试替身。**首次出现**：[第 14 章](14-可观测Trace日志回放.md#3、内核harnesstracepy第十二块砖) |
| 安全三件事 | 终止条件（该停停得住）+ 最小授权/审批（不该碰碰不了）+ sandbox（坏东西跑不出圈）。**首次出现**：[第 15 章](15-终止条件权限安全.md#1、安全三件事--一件事) |
| 最小授权 | 只给够用的权限（read < write < execute），越权即拒。**首次出现**：[第 15 章](15-终止条件权限安全.md#2、最小授权--审批--sandbox三件套) |
| `PolicyGuard` / `ToolPolicy` | 工具层的鉴权 + 审批 + sandbox 执行点；包装 Tool，MiniAgent 零改动。**首次出现**：[第 15 章](15-终止条件权限安全.md#3、内核harnesssafetypy第十三块砖) |
| `DenySandbox` | 危险操作黑名单（rm -rf / 读密码 / 起子进程等），命中即拒。**首次出现**：[第 15 章](15-终止条件权限安全.md#3、内核harnesssafetypy第十三块砖) |
| Prompt Injection | 把指令藏进不可信数据诱导 Agent 执行；对策是「别把不可信内容当指令」。**首次出现**：[第 15 章](15-终止条件权限安全.md#1、安全三件事--一件事) |
| `detect_injection` / `InjectionReport` | 提示注入启发式检测 + 结果（可疑/命中词/得分）。**首次出现**：[第 15 章](15-终止条件权限安全.md#3、内核harnesssafetypy第十三块砖) |
| MCP（Model Context Protocol） | 工具「谁定义（Server）/谁消费（Client）」解耦的协议；客户端把远程工具适配成本框架 Tool。**首次出现**：[第 16 章](16-MCP-A2A-Skills接入.md#2、mcp工具谁定义与谁消费解耦) |
| `MCPServer` / `MCPClient` | 极简 MCP 两端：暴露 tools/list + tools/call；把远程工具适配成 Tool。**首次出现**：[第 16 章](16-MCP-A2A-Skills接入.md#5、内核harnessprotocolpy第十四块砖) |
| A2A（Agent-to-Agent） | Agent 之间协作：把「另一个 Agent」包装成工具来调。**首次出现**：[第 16 章](16-MCP-A2A-Skills接入.md#3、a2a专家也是一种工具) |
| `AgentEndpoint` / `a2a_tool` | 一个 Agent 的对外接口 + 把它包装成 Tool 的适配器。**首次出现**：[第 16 章](16-MCP-A2A-Skills接入.md#5、内核harnessprotocolpy第十四块砖) |
| `Skill` / `SkillLibrary` | 技能资产（提示词+工具+用法）与技能库（catalog + 按需 activate）。**首次出现**：[第 16 章](16-MCP-A2A-Skills接入.md#4、skills按需挂载别一次塞满) |

## 四、RAG 与检索

| 术语 | 一句话解释 |
| --- | --- |
| Embedding | 把文本映射成向量 |
| 向量检索 | 按向量相似度找最相关内容 |
| Chunking | 把长文档切成小块 |
| Rerank | 对召回结果二次排序 |
| Hybrid Retrieval | 稠密 + 稀疏混合检索 |
| RAG | 检索增强生成 |

## 五、协议与工程化

| 术语 | 一句话解释 |
| --- | --- |
| MCP | Model Context Protocol，模型与外部工具的标准化协议 |
| A2A | Agent-to-Agent，跨 Agent 互操作协议 |
| Skills | 可复用的操作知识资产 |
| Eval | 评测框架，离线/在线、过程/结果指标 |
| Prompt Injection | 通过注入指令劫持模型行为 |
| Sandbox | 限制 Agent 行为影响面的安全边界 |
