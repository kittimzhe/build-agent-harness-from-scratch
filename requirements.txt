# 核心依赖：足以离线跑通全部主线案例 + CI（examples 06–16、17 离线、pytest 回归）
# 向量库是手写的纯 math 实现，不需要 numpy。
# 需要 FastAPI 冒烟（18 章）时再装 requirements-full.txt

# OpenAI 兼容 SDK（DeepSeek/通义/OpenAI/Ollama 通用）
openai>=1.50.0

# 环境变量加载（find_dotenv 支持在子目录运行）
python-dotenv>=1.0.0

# 结构化输出与数据校验
pydantic>=2.7.0
