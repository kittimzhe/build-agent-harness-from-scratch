# 核心依赖（第 01–07 章够用）
# 装这一个文件即可跑通第 01 章及前几章案例。
# 阶段三起的能力（向量库 / 文档加载 / 部署）见 requirements-full.txt

# OpenAI 兼容 SDK（DeepSeek/通义/OpenAI/Ollama 通用）
openai>=1.50.0

# 环境变量加载（find_dotenv 支持在子目录运行）
python-dotenv>=1.0.0

# 结构化输出与数据校验
pydantic>=2.7.0
