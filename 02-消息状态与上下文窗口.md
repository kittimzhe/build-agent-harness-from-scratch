# 02 - 消息状态与上下文窗口

> 📌 **第 2 章 · 阶段一 Prompt 层** · [← 返回目录大纲](教程目录大纲.md) · [上一章 01 LLM 调用与环境准备 →](01-LLM调用与环境准备.md) · 下一章 03 Prompt 与结构化输出（规划中）

---

**本章课程目标：**

- 理解大模型 API 的本质：**无状态**。模型不记得你——每轮都靠你把全部历史重发一遍。所谓「对话」，就是维护一个不断增长的消息列表。
- 亲手把消息历史封装成 `ChatSession`（Harness 第二块砖），并学会读 `usage`、算多轮对话的账单。
- 搞懂上下文窗口与 KV Cache：窗口爆了会怎样、为什么长上下文不是万能药。
- 打出历史治理的第一刀：**截断**——便宜、快，但会忘事。这个矛盾正是第 08 章 Context 治理要解决的。

**学习建议：** 这一章是「从 Prompt 走向 Context Engineering」的起点。重点不是 API 怎么调，而是建立一个观念：**上下文不是静态输入，而是动态演化的状态**。写代码时多问自己一句：这一轮我到底该给模型看什么？

**官方文档与资源**：详见 [工具导航与参考资料索引](工具导航与参考资料索引.md)。

---

## 1、消息列表就是 Agent 的第一个状态

### 1.1 先戳破一个错觉：模型「记得」你？

跑通第 01 章后，你可能以为模型认识你了。做个实验——每次都只发一条新消息、不带历史：

```python
from harness import LLMClient

llm = LLMClient()
llm.chat([{"role": "user", "content": "我叫小明"}])
reply = llm.chat([{"role": "user", "content": "我叫什么名字？"}])  # 不带历史
print(reply.content)   # 模型：抱歉，你还没告诉我你的名字
```

模型不记得。**Chat API 是无状态的**：服务端不保存任何对话，每一轮的「记忆」，全靠客户端把历史消息原样再发一遍。你在各种产品里体验到的「多轮对话」，底层就是这个消息列表在滚雪球：

```python
messages = [
    {"role": "system",    "content": "你是一个简洁的助手"},
    {"role": "user",      "content": "我叫小明"},
    {"role": "assistant", "content": "你好小明！"},
    {"role": "user",      "content": "我叫什么名字？"},   # 新一轮
]
```

> 💡 这就是本章的核心观念：**消息列表 = Agent 的第一个状态**。第 05 章的工具循环、第 12 章的 Checkpoint，本质上都在管理某种状态——而它们全都会回到「这一轮该把什么放进消息列表」这个问题。

### 1.2 三种消息角色

| 角色 | 谁写的 | 作用 |
| --- | --- | --- |
| `system` | 开发者 | 定人设、给规则。**始终在最前面**（这对 4.2 节的 KV Cache 很重要） |
| `user` | 用户（或代替用户的程序） | 提问、下指令 |
| `assistant` | 模型 | 历史回复。下一轮请求时原样带上 |

> 预告：第 04 章会加第四种角色 `tool`（工具返回结果）。届时消息列表就从「对话记录」升级成「执行日志」——Agent 的雏形就在那里。

---

## 2、封装 ChatSession：第二块砖

裸写消息列表两轮就乱。和第 01 章封装 `LLMClient` 同一个理由：状态要有人管。这就是 Harness 的第二块砖——`ChatSession`。

> 这 60 行代码，对应框架里的什么？

| 本教程 | 框架里对应的东西 |
| --- | --- |
| `ChatSession` | LangChain 的 `ChatMessageHistory` / Memory，OpenAI Agents SDK 的 Session |
| `messages` 列表 | 框架里托管的对话 state |
| `total_usage` 累积 | 框架里的 usage 回调 / token 计数器 |
| `truncate(keep_last_n)` | LangChain 的 `trim_messages`、各类窗口记忆策略 |

### 2.1 设计要点

1. **状态只有一份**：历史就放在 `session.messages` 里，别在业务代码里再自己拼一份。
2. **`ask()` 一个方法完成一轮**：追加 user → 调模型 → 追加 assistant → 累积 usage。调用方不碰消息拼装的细节。
3. **`ask()` 签名从本章起冻住**：后面加能力（比如第 08 章的压缩治理）只加新方法，不改它。

### 2.2 实现

完整代码见 [harness/session.py](harness/session.py)。核心长这样：

```python
class ChatSession:
    def __init__(self, llm=None, system=None):
        self.llm = llm or LLMClient()
        self.messages = []
        if system:
            self.messages.append({"role": "system", "content": system})
        self.total_usage = {"prompt_tokens": 0, "completion_tokens": 0, "total_tokens": 0}

    def ask(self, user: str, **kwargs) -> str:
        """问一轮：追加 user → 调模型 → 追加 assistant → 累积用量。"""
        self.messages.append({"role": "user", "content": user})
        result = self.llm.chat(self.messages, **kwargs)
        self.messages.append({"role": "assistant", "content": result.content})
        for k in self.total_usage:
            self.total_usage[k] += result.usage.get(k, 0)
        return result.content
```

### 2.3 用起来

```python
from harness import ChatSession, LLMClient

session = ChatSession(LLMClient(), system="你是一个简洁的助手")
session.ask("我叫小明，今年 24 岁。")
session.ask("我最喜欢的语言是 Python。")
print(session.ask("我叫什么？多大了？喜欢什么语言？"))   # 三件事都记得
print(session.total_usage)   # 两轮累积的 token 账单
```

> 🧱 第二块砖落位：`LLMClient`（调用层）+ `ChatSession`（状态层）。对照第 01 章的架构图，我们正在从底往上把 Context 层砌起来。

---

## 3、Token 与成本：多轮对话的隐藏账单

### 3.1 usage 怎么读

每次调用的返回里都带 `usage`（第 01 章已收进 `LLMResult`）：

- `prompt_tokens`：**这一轮输入**消耗的 token（= system + 全部历史 + 新问题）
- `completion_tokens`：这一轮输出消耗的 token
- `total_tokens`：两者之和

### 3.2 关键认识：多轮对话的成本是滚雪球的

因为每轮都要重发全部历史，第 N 轮的 `prompt_tokens` ≈ 前面所有轮的总和。跑一下本章案例的 demo②，你会亲眼看到：

```
第 1 轮 prompt_tokens 增量: +27
第 2 轮 prompt_tokens 增量: +86
第 3 轮 prompt_tokens 增量: +150
```

每轮增量都在变大——对话越长，每一轮越贵。**这是后面所有「上下文治理」的经济动因**：压缩、摘要、按需读取，省的不只是窗口，还有真金白银。

> 💡 面试高频：为什么多轮 Agent 的成本控制要从上下文入手？答案就从这张账单讲起。

---

## 4、上下文窗口：天花板与 KV Cache

### 4.1 窗口爆了会怎样

上下文窗口 = 模型一次能「看到」的 token 上限（输入 + 输出共享）。历史滚到超过窗口，API 直接报错：

```
Error code: 400 - This model's maximum context length is 65536 tokens.
However, you requested ... tokens ...
```

不治理历史的聊天循环，跑得越久越接近这颗雷。处理思路见第 5 节（截断），报错排查见第 7 节。

### 4.2 KV Cache：为什么「前缀稳定」能省钱提速

KV Cache 是**推理侧**的优化：模型算过一遍的前缀（比如很长的 system + 历史），中间结果（Key/Value）被缓存下来，下一轮前缀不变的部分不用重算——更快，很多提供商（如 DeepSeek 的上下文缓存）还会对命中缓存的部分**打折**。

这带来一条工程守则：

> **把稳定的内容放前面（system、工具定义），把多变的内容放后面（最新对话）。** 前缀越稳定，缓存命中率越高。

这也是为什么 `ChatSession` 把 `system` 固定在最前面、截断时永远保留它。

> 辨析（面试常考）：**KV Cache / Prefix Caching** 是推理引擎或提供商服务端的缓存；有些框架里说的 Prompt Caching 是 API 层显式声明缓存前缀。机制类似，层次不同。

### 4.3 为什么长上下文不是万能药

「窗口不是有 128K 吗，随便聊」——三个反例：

1. **注意力退化**：塞得越多，模型对中段信息的利用率越低（lost in the middle 现象），回答质量不升反降。
2. **成本线性涨**：3.2 节的账单，窗口大只是让你「付得起更久的雪球」，不是免单。
3. **延迟涨**：输入越长，首 token 越慢，体验变差。

> 💡 所以正确姿势不是「窗口大就随便塞」，而是「每轮只给模型**最有用的那部分 tokens**」——这正是 Context Engineering 的定义，第 08 章展开。

---

## 5、历史治理第一刀：截断

最简单粗暴也最常用的治理：**只保留 system + 最近 N 条消息**。

```python
session.truncate(keep_last_n=6)   # 保留 system + 最近 6 条 ≈ 最近 3 轮
```

实现就三行：system 单独摘出来，剩下的取尾部，拼回去。

**代价也明显**：截断点之前的记忆直接丢失——案例 demo③ 里，截断后模型就忘了「小明」。便宜 vs 记忆，这是治理的第一对矛盾：

| 治理手段 | 成本 | 记忆 | 出场章节 |
| --- | --- | --- | --- |
| 截断 | 极低 | 丢早期 | 本章 |
| 压缩 / 摘要 | 低（一次摘要调用） | 有损保留 | 第 08 章 |
| 外置记忆 + 按需读取 | 中 | 可全量保留 | 第 09 章 |

---

## 6、运行本章案例

确保已完成[第 01 章环境准备](01-LLM调用与环境准备.md#3、环境准备)，然后在仓库任意目录运行：

```bash
python examples/02_chat_history.py
```

预期输出（三个 demo）：

```
✅ 环境自检通过：provider=deepseek

====================================================
① 多轮对话：消息就是状态
====================================================
用户：我叫小明，今年 24 岁。
模型：你好，小明！
用户：我最喜欢的编程语言是 Python。
模型：记住了，你喜欢 Python。
用户：我叫什么名字？多大了？
模型：你叫小明，今年 24 岁。

(此刻历史里有 6 条消息，3 个回合)

====================================================
② Token 与成本：多轮对话的隐藏账单
====================================================
第 1 轮 prompt_tokens 增量: +27
第 2 轮 prompt_tokens 增量: +86
第 3 轮 prompt_tokens 增量: +150

三轮累积用量: {'prompt_tokens': ..., 'completion_tokens': ..., 'total_tokens': ...}
💡 每轮都把全部历史重发给模型——这就是多轮对话成本越滚越大的原因

====================================================
③ 历史治理第一刀：截断
====================================================
截断前：6 条消息
截断后：3 条消息
模型：抱歉，你还没告诉我你的名字。
💡 便宜、快，但早期记忆没了——第 08 章 Context 治理会解决这个矛盾
```

> 💡 demo③ 截断后模型「忘了小明」不是 bug，是截断的天然代价——亲眼看到它，第 08 章的动机就有了。

---

## 7、常见报错排查

| 报错 | 原因 | 解决 |
| --- | --- | --- |
| `maximum context length ... tokens`（400） | 历史滚过窗口上限 | 先 `truncate()`；根治见第 08 章治理策略 |
| `Invalid message[0]` 之类角色报错 | system 没放第一条，或角色拼错 | system 永远在最前；角色只能是 system/user/assistant（第 04 章才有 tool） |
| 模型「失忆」 | 要么没带历史（每轮新建了 messages），要么被 truncate 剪掉 | 用 `session.ask()` 走完整循环，别绕过它自己拼消息 |
| `completion_tokens` 为 0 或回复为空 | 触发某些过滤 / max_tokens 设太小 | 检查 `max_tokens` 参数；换个问法 |

更多见 [新手入门与常见问题](新手入门与常见问题.md)。

---

## 8、本章小结与下一章

✅ 你现在已经能：

- 说清 Chat API 无状态的本质：**消息列表就是 Agent 的第一个状态**
- 用 `ChatSession` 管历史、读 `usage`、算多轮对话的滚雪球账单
- 解释上下文窗口上限、KV Cache 与「前缀稳定」守则、长上下文的三个坑
- 用截断打出历史治理第一刀，并说出它的代价

➡️ 按目录下一章是 **03 Prompt 与结构化输出**（规划中）。**已发布的下一站：[第 05 章《手写第一个工具循环》 →](05-手写第一个工具循环.md)**——消息列表会升级成执行日志（`tool` 角色登场），你会亲手写出 Agent 的心脏。可回[教程目录大纲](教程目录大纲.md)看全局。
