# 17 - 实战项目：深度研究助手 Agent

> 📌 **第 17 章 · 阶段六 Protocol 与实战** · [← 返回目录大纲](教程目录大纲.md) · [上一章 16 MCP/A2A/Skills →](16-MCP-A2A-Skills接入.md) · [下一章 18 部署交付：FastAPI + Docker →](18-部署交付FastAPIDocker.md)

---

**本章课程目标：**

- 把前十六章的**主线砖**串成一个真实项目：一个「深度研究助手」——联网搜索 + RAG + 多步规划 + 报告生成。
- 看清深度研究 agent 的**三层数据流**：Phase 1 规划 → Phase 2 检索 → Phase 3 综合。
- **实打实接线四样**（这也是实战与 demo 的分界线）：Checkpoint（断点续跑）、Trace（可观测）、终止条件（护栏）、记忆（边搜边记）。其余砖（MCP/A2A、`retry_with_reflection`、`compact`、`PolicyGuard` 等）**标为「未接入/可替换」**，不冒充已用。
- 学会「离线替身 → 换真」的交付方式：`FakeSearchEngine`/`ScriptedLLM` 跑通全流程后，只换 `LLMClient`/真实搜索，代码同一份。

**学习建议：** 本项目**离线跑通不需要 API Key**（假搜索引擎 + 脚本模型）。先把三层数据流跑通，再谈真 API。

---

## 1、深度研究 = 规划 + 检索 + 综合

「深度研究」（Deep Research）类 agent 的打法就三句话：

1. **先规划**：把一个大问题拆成几个可分别查证的子问题（第 10 章）。
2. **再检索**：每个子问题搜一下、记下来；搜不到就换词再搜（第 05/09/11 章）。
3. **最后综合**：把检索到的片段拼起来，交给 LLM 写报告（第 08/10 章）。

```
问题 ──Phase 1 规划(10)──▶ 子问题列表
                ──Phase 2 检索(05/09/11/12)──▶ 搜索 + 记笔记 + 反思 + checkpoint
                ──Phase 3 综合(08/09/10)──▶ 向量取片 + 预算裁剪 + LLM 写报告
全程：trace(14) + 终止条件/护栏(15)
```

与前面每一章 `MiniAgent` 的区别：**这不是「一个循环」，是一个「流程编排」**——几步之间要能断点续跑（12）、要全程可观测（14）、输出要有护栏（15）。

---

## 2、为什么「实打实接线 Checkpoint / Trace / 终止条件」

demo 和实战的分界线，就落在这四样：

| 接线项 | 不用会怎样 | 用在哪 |
| --- | --- | --- |
| **Checkpoint**（12） | 搜到第 4 步崩了，前 3 步白干 | `run_plan_with_checkpoint` 每步落盘，`resume=True` 只重跑没 done 的 |
| **Trace**（14） | 报告写错了，查不出哪一步出的 | `Tracer` 包 LLM，`trace.jsonl` 全程留痕可回放 |
| **终止条件**（15） | 报告越写越长 / 步骤失控 | 计划步数 + `StopConditions` 设输出预算 |
| **记忆**（09） | 搜完就忘，重复内容堆进 prompt | 文件记忆存笔记、向量记忆做检索去重 |

> 一句话：**流程能重启（Checkpoint）、过程能复现（Trace）、失控能叫停（终止条件）、素材能沉淀（记忆）**，才谈得上「上线」。

---

## 3、项目结构与数据流

```
projects/deep_research/
├── search.py     # 检索层：FakeSearchEngine（离线）+ make_search_tool（签名统一）
├── agent.py      # DeepResearchAgent：三阶段编排 + 四大工件落盘
├── main.py       # CLI：--offline 离线跑通 / --question 真实研究
└── README.md     # 项目说明 + 怎么换真
```

- `search.py`：把搜索包成一条 `Tool`（第 05/16 章「能力适配成 Tool」的最小实践）。离线 `FakeSearchEngine` 在教程概念小语料上确定性检索；真换成 Tavily/Bing 只改 engine，`make_search_tool` 签名不变。
- `agent.py`：`DeepResearchAgent.research(question) -> {report, plan, metrics, ...}`，三段走完，落盘四大工件。
- `main.py`：`--offline` 用 `ScriptedLLM` + `FakeSearchEngine` 跑全流程；`--question` 走真 API。

---

## 4、内核复用清单（接入了什么、没接入什么）

| 章 | 砖 | 状态 | 在本项目的落点 |
| --- | --- | --- | --- |
| 05 | `Tool` / `AgentLoop` | ✅ 已接入 | `search_tool` 就是一条 Tool |
| 08 | Context 治理 | ⚠️ 简化版 | `_assemble_context` 只做字符截断；`compact`/`select_by_budget` 未接（留给读者换真） |
| 09 | 记忆 / RAG | ✅ 已接入 | `FileMemory` 记笔记 + `VectorMemory` 检索去重 |
| 10 | Planning | ✅ 已接入 | `make_plan`（真实路径）/ 注入 plan（离线） |
| 11 | 反思 | ⚠️ 简化版 | 空结果走**确定性改词** `_reformulate`（去停用词）；`reflect`/`retry_with_reflection` 未接 |
| 12 | Checkpoint | ✅ 已接入 | `run_plan_with_checkpoint` + `resume=True` |
| 14 | Trace | ✅ 已接入 | `Tracer(wrap=llm)`，`trace.jsonl` 落盘 |
| 15 | 终止条件 | ✅ 已接入 | `StopConditions`：计划步数预算（检索前拦）+ 输出字符预算 |
| 16 | MCP / A2A / Skills | ❌ 未接入 | `search_tool` 可换成 `MCPClient.to_harness_tools()`（可替换，不在主路径） |
| 06 | `ResilientTool` | ❌ 未接入 | 真接 Tavily/Bing 时套上（可替换） |
| 02 | `ChatSession` | ❌ 未接入 | 深度研究不是对话，无需会话态 |

> 看这个表你会发现：**第 17 章没有新砖，也没把每一块砖都用上**。本章的「新」不在内核，在**把已接入的主线砖拼成一个能交付的流程**；「未接入」的留给读者按 §6 的换真路径自己接。

---

## 5、运行案例

无需 `.env`，先离线跑通三层数据流：

```bash
python projects/deep_research/main.py --offline
```

预期输出（确定性）：

```
【Phase 1 规划】子问题 = ['LangGraph 是什么', 'MCP 是什么', '它们怎么配合', '量子引力的拓扑结构']
【Phase 2 检索】各步状态：
  [done] LangGraph 是什么
  [done] MCP 是什么
  [done] 它们怎么配合
  [done] 量子引力的拓扑结构
  （第 4 步语料必空 → 走『空结果→改词重搜』：见 notes.json step3）
【Phase 3 综合】报告：
  ## 研究报告
  ### LangGraph 是什么
  LangGraph 是 LangChain 出的图式 Agent 编排框架…
  ...
【指标】 {...}
【工件】checkpoint: .deep_research_demo/checkpoint.json
        notes: .deep_research_demo/notes.json
        trace: .deep_research_demo/trace.jsonl

断点续跑：resume=True 从 checkpoint 恢复
  恢复后的步骤状态： [done] [done] [done] [done]
  （全部 done —— checkpoint 让第二次调用零重跑）
```

配好 API 后走真实研究：

```bash
python projects/deep_research/main.py --question "你的研究课题"
```

> **盯三个地方**：① 第 4 步「量子引力的拓扑结构」在语料里必空，于是**真实走了**「空结果 → `_reformulate` 改词重搜」的反思路径（打开 `notes.json` 的 step3 能看到改词轨迹）；② `resume=True` 第二次调用时四步全是 `done`、零重跑——第 12 章的 checkpoint 展现出「赔得起的重试」价值；③ 三件工件（checkpoint/notes/trace）落盘——第 09/12/14 章的资产不再是概念，是文件。

---

## 6、常见报错排查

| 报错 / 现象 | 原因 | 解决 |
| --- | --- | --- |
| `ModuleNotFoundError: No module named 'harness'` | 没把仓库根加进 sys.path | 用 `python projects/deep_research/main.py` 从仓库任意处跑（main.py 已处理） |
| 离线报告永远一样 | 用了 `ScriptedLLM` | 正常——离线就是确定性；换 `--question` 走真 LLM |
| 真实研究报 API key 错 | `.env` 没配或没加载 | 按第 01 章配 `.env`，main.py 已 `find_dotenv` |
| `resume=True` 步骤状态不对 | 换了 workdir / checkpoint 不在 | 断点续跑要用**同一个 workdir** |
| 空结果步骤没触发换词 | 判定用的是精确串比较（`text.strip() == "（无相关结果）"`） | 确认搜索工具空结果时返回的正是这串文案；demo 第 4 步保证会触发 |

更多见 [新手入门与常见问题](新手入门与常见问题.md)。

---

## 7、本章小结与下一章

✅ 你现在已经能：

- 说清深度研究 agent 的「规划 → 检索 → 综合」三层数据流
- 把主线砖接线成一个能跑通全流程的 `DeepResearchAgent`，并说清哪些「未接入/可替换」
- 说清为什么实战强推 Checkpoint / Trace / 终止条件 / 记忆
- 用「离线替身 → 换真」的方式交付，代码同一份

➡️ 下一章 [**18 部署交付：FastAPI + Docker →**](18-部署交付FastAPIDocker.md)（阶段六收官）：项目跑通了，离「上线」还差一层——把它包成一个 HTTP 服务（FastAPI），再打进 Docker 镜像，从 demo 到可被任何人调用的服务。可回[教程目录大纲](教程目录大纲.md)看全局。