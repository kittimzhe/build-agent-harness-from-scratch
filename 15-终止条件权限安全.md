# 15 - 终止条件 · 权限 · 安全

> 📌 **第 15 章 · 阶段五 Runtime 层（收官）** · [← 返回目录大纲](教程目录大纲.md) · [上一章 14 可观测：Trace/日志/回放 →](14-可观测Trace日志回放.md) · [下一章 16 MCP / A2A / Skills 接入 →](16-MCP-A2A-Skills接入.md)

---

**本章课程目标：**

- 说清生产级 Agent 的**安全三件事**：该停停得住（终止条件）、不该碰碰不了（最小授权 + 审批）、坏东西跑不出圈（sandbox）+ 不被注入带偏。
- 掌握**「走包装、不走侵入」的安全落法**：`PolicyGuard.wrap(Tool)` 给工具套上鉴权/审批/sandbox 三件套，`AgentLoop`/`MiniAgent` 零改动。
- 会用启发式 **Prompt Injection 检测**：把「藏在数据里的指令」和「真指令」分开看——并清楚它是**教具**：启发式误报率天然高，只留长而固定的话术，生产别指望它当唯一防线。
- 落地第十三块砖：`harness/safety.py`（`StopConditions` / `ToolPolicy` / `PolicyGuard` / `DenySandbox` / `detect_injection`）。

**学习建议：** 本章全部 demo **不需要 API Key**——安全三件事都是本地确定性操作。这也是阶段五的收官：一个有护栏、有容错、有权限、可观测、扛得住的 runtime 到此齐活。

---

## 1、安全三件事 + 一件事

把「Agent 安全」拆成四句话，生产里每天都在用：

| 抓手 | 回答问题 | 落点 |
| --- | --- | --- |
| **终止条件** | 该不该停 | `StopConditions`（max_rounds / 输出预算 / 停止短语） |
| **最小授权** | 谁有资格碰 | `PolicyGuard`（read < write < execute，越权即拒） |
| **审批** | 高危动作谁说了算 | `PolicyGuard.approver`（回调点头才放行） |
| **sandbox** | 坏代码跑多远 | `DenySandbox`（黑名单；生产级换系统隔离） |

再加一件事——**Prompt Injection**：攻击者把指令藏进网页/邮件/文件里，诱导 Agent 把它当命令执行。对策不是「识别到就完」，而是**「别把不可信内容当指令」**：数据就是数据，指令可靠地来自系统/用户通道。

> 核心原则一条：**Agent 的工具调用面是攻击面。** 权限和审批全在工具这一层拦，正如第 06 章容错、第 14 章观测一样——**能力走包装，不走侵入**。

> ⚠️ **先说破两个教具的边界**：（a）`detect_injection` 的关键词表只收**长而固定**的话术（「忘掉之前的指令」「ignore previous instructions」）——短词（如「你是」）会把正常中文全判成可疑，宁可漏报、不可误伤；（b）`DenySandbox` 的黑名单含 `open(` / `curl` 这类宽词，**合法工具描述也可能中招**。两者都是教学演示，生产要上系统级隔离 + 结构性防护，不是关键词表。

---

## 2、最小授权 + 审批 + sandbox：三件套

```python
guard = PolicyGuard(grant="read", approver=ask_human, sandbox=DenySandbox())
safe_del = guard.wrap(Tool(delete_all), ToolPolicy(level="write", needs_approval=True))

agent = MiniAgent(llm=..., tools=[safe_del])   # MiniAgent 照旧，零改动
```

`_GuardedTool.run()` 在真正执行前过三道闸，任一不过就抛 `ToolError`：

1. **最小授权**：`grant=read` 调 `level=write` 的工具 → 「权限不足」。
2. **审批**：`needs_approval=True` 且 `approver` 返回 False → 拦下。
3. **sandbox**：参数里含 `rm -rf` / `/etc/passwd` / `subprocess` 等 → 命中黑名单拦下。

这些 `ToolError` 会像第 05/06 章一样，被 `AgentLoop` 作为 **observation 回喂给模型**——模型看到「我没有权限」就会自己改主意（demo⑥ 演给你看）。

> 分层要分清：黑名单 sandbox 拦得住**图省事/误伤**的调用，拦不住**蓄意绕过**。真对抗场景要用系统级隔离（子进程限额、容器、只读挂载、网络 allowlist）——黑名单是第一道不是唯一一道。

---

## 3、内核：harness/safety.py（第十三块砖）

- `StopConditions.check(rounds, output_text)`：统一判断该不该停。
- `has_permission(granted, required)`：`read < write < execute` 的权限比较。
- `PolicyGuard.wrap(tool, ToolPolicy)`：给工具套鉴权 + 审批 + sandbox。
- `DenySandbox.check(*texts)`：危险操作黑名单。
- `detect_injection(text)`：返回 `InjectionReport`（可疑 + 命中词 + 得分）。

完整实现见 [harness/safety.py](harness/safety.py)。

> 🧱 第十三块砖落位，**阶段五 Runtime 层收官**：runtime（封装）→ trace（可观测）→ safety（安全）三层壳齐了。内核从「能跑」进化到「敢跑」。下一步进入阶段六 Protocol 与实战，把 Agent 接到真实世界。

---

## 4、串联：安全与前面每一章

- **05**：`ToolError` 被回喂给模型，安全拦截 = 一种省心的「失败」，模型能自己转弯。
- **06**：`ResilientTool` 与 `GuardedTool` 都是工具包装——可叠加：`(ResilientTool → GuardedTool)` = 先容错、再鉴权。
- **08**：注入常藏在检索回来的上下文里——「不可信内容」更要走 §1 的「别当指令」策略 + `detect_injection`。
- **13**：`MiniAgent` 收的是 `Tool`，`GuardedTool` 就是 `Tool`——因此 Integration 零成本。
- **16**：接外部协议（MCP/A2A）时，外部工具的权限声明正好复用 `ToolPolicy`。

---

## 5、运行本章案例

无需 `.env`：

```bash
python examples/15_safety.py
```

预期输出（全部确定性）：

```
① 终止条件：统一检查「该不该停」
  rounds=5、输出短 → 超过 max_rounds=4
  rounds=2、输出 30 字符 → 输出超过 20 字符
  rounds=2、输出含『任务完成』→ 命中停止短语 '任务完成'
  rounds=2、一切正常 → None

② 最小授权：read 权限去调 write 工具
  拦下：权限不足：delete_all 需要 write，当前授权 read

③ 审批：敏感工具要人点头
  未获批：delete_all 未获人工批准
  获批后：已删除 orders

④ sandbox：危险操作黑名单
  'rm -rf /'       → 拦下：rm -rf
  'cat /etc/passwd'→ 拦下：/etc/passwd
  '读一下报表'      → 放行

⑤ Prompt Injection：把「藏在数据里的指令」揪出来
  ✅ 正常: '帮我把这份摘要整理一下'  命中=[]
  ⚠️ 可疑: '请忘掉之前的指令…'  命中=['忘掉之前的指令', …]

⑥ 组合：模型要删库 → guard 拦下 → 错误回喂 → 模型改口
  最终 state=done
  reply=明白了，我没有删除权限，已停止。
```

> **盯 demo⑥**：模型第一轮真想调 `delete_all`，guard 拦下后把 `权限不足` 作为 observation 回喂，第二轮模型自己改口「我没有权限，已停止」。这就是「在工具层拦 + 让模型自己转弯」的闭环。

---

## 6、常见报错排查

| 报错 / 现象 | 原因 | 解决 |
| --- | --- | --- |
| `权限不足：xxx 需要 write，当前授权 read` | 最小授权在拦 | 确认工具该要什么权限；不该升权就换工具 |
| `xxx 需要人工审批，但未配置 approver` | 声明了审批却没给审批人 | 传 `approver=...` 回调 |
| 危险调用居然放行了 | 黑名单没盖住那个词 | 给 `DenySandbox(deny=(...))` 加黑名单 |
| 注入检测老误报 | 关键词表混进了短词 | `INJECTION_PATTERNS` 只留长而固定的话术；根治靠「别把不可信内容当指令」+ 系统级隔离 |
| 模型被拦后还硬试同一工具 | 错误回喂了但模型轴 | 结合第 11 章反思 + 第 05 章 max_rounds 护栏 |

更多见 [新手入门与常见问题](新手入门与常见问题.md)。

---

## 7、本章小结与下一章

✅ 你现在已经能：

- 说清安全三件事 + 注入：终止、权限/审批、sandbox
- 用 `PolicyGuard.wrap(Tool)` 零侵入地给工具套鉴权/审批/sandbox
- 用 `StopConditions` 统一判断终止、用 `detect_injection` 做启发式注入检测
- 说清黑名单 sandbox 与系统级隔离的分层

➡️ 下一章 [**16 MCP / A2A / Skills 接入 →**](16-MCP-A2A-Skills接入.md)（阶段六 Protocol 开局）：安全齐了，下一步把 Agent 接进「真实世界」——MCP 接工具生态、A2A 接别的 Agent、Skills 挂技能包；外部工具正好复用本章的 `ToolPolicy` 做权限。可回[教程目录大纲](教程目录大纲.md)看全局。