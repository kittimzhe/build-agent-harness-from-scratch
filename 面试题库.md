# 面试题库

本题库对标 2026 「AI Agent / 大模型应用开发工程师」岗位高频考点，按能力域组织「问法 + 答法要点」。题目来源覆盖大厂真实面经与高频追问，并与正文章节互链。

> 答题核心主线：**工程重心已从 Prompt 迁移到 context、harness、eval、protocol、runtime。** 讲项目时少说「我用了 ReAct/RAG/LangChain」，多说「我怎么治理上下文、怎么设计工具 schema、怎么做 checkpoint 和 trace、怎么定终止条件和权限」。

---

## 〇、已发布章节可覆盖的题（带答法要点）

### 第 01 章（调用层）

- **Q：接入一个新的大模型，最少要准备哪几样东西？**
  - 答：**调用三件套**——API Key、模型名、Base URL。只要是兼容 OpenAI 协议的提供商（DeepSeek/通义/OpenAI/Ollama），同一套 SDK 代码只换这三个值就能跑。→ [第 01 章 · 第 2 节](01-LLM调用与环境准备.md#2、调用三件套api-key模型名base-url)

- **Q：同步调用和流式调用怎么选？流式调用有什么坑？**
  - 答：面向用户的对话/Agent 中间步骤用流式，后台批处理用同步。流式首块 `delta.content` 可能为 `None`，要 `or ""` 兜底。→ [第 01 章 · 第 4 节](01-LLM调用与环境准备.md#4、第一次调用同步-流式)

- **Q：为什么这么多国产模型都能用 OpenAI SDK 调？**
  - 答：OpenAI 的 Python SDK 成了事实标准，DeepSeek、通义、Ollama 主动兼容它，所以换 provider 只是换三个配置值，不动业务代码。→ [第 01 章 · 第 2.2 节](01-LLM调用与环境准备.md#22-为什么是同一套代码)

- **Q：什么是 Agent Harness？为什么 2026 这么强调它？**
  - 答：Harness 是包裹 LLM 的运行时脚手架——状态、工具、checkpoint、trace、权限、终止条件。重心从 Prompt 迁到 harness，是因为模型更强后，长任务的可靠性、可恢复、可审计成了真问题。→ [第 01 章 · 第 1 节](01-LLM调用与环境准备.md#1、为什么这份教程不用现成框架)

- **Q：为什么这个教程不用 LangChain，而是从零手写？**
  - 答：不是否定框架，而是「亲手实现过 Harness，才能看懂框架替你做了什么」。本教程把 `LLMClient` 这层抽象拆开，对应 `ChatOpenAI` / `model=`，看清那 10 行，再用任何框架不发懵。→ [第 01 章 · 第 5 节](01-LLM调用与环境准备.md#5、封装-llmclient第一块-harness-砖)

### 第 02 章（状态层）

- **Q：大模型 API 是有状态还是无状态？多轮对话的「记忆」从哪来？**
  - 答：**无状态**。服务端不保存对话，每轮的「记忆」全靠客户端把全部历史消息重发一遍——所以消息列表就是 Agent 的第一个状态，管理它是工程问题不是模型问题。→ [第 02 章 · 第 1 节](02-消息状态与上下文窗口.md#1、消息列表就是-agent-的第一个状态)

- **Q：多轮对话为什么越聊越贵？成本怎么控制？**
  - 答：每轮 prompt_tokens ≈ 前面所有轮的总和，成本滚雪球。控制手段从上下文入手：截断、压缩摘要、外置记忆按需读取——这是 Context Engineering 的经济动因。→ [第 02 章 · 第 3 节](02-消息状态与上下文窗口.md#3、token-与成本多轮对话的隐藏账单)

- **Q：KV Cache / Prefix Caching 是什么？对写 Agent 有什么实际影响？**
  - 答：推理侧缓存「已算过的前缀」，命中则更快更便宜（部分提供商打折）。工程守则：**稳定内容放前面**（system、工具定义），多变内容放后面，提高前缀命中率。→ [第 02 章 · 第 4.2 节](02-消息状态与上下文窗口.md#42-kv-cache为什么前缀稳定能省钱提速)

- **Q：窗口都 128K 了，为什么还要做上下文治理？**
  - 答：三个坑——注意力退化（lost in the middle，塞越多中段利用率越低）、成本线性涨、首 token 延迟涨。窗口大只是「付得起」，不是「免单」，正确姿势是每轮只给最有用的 tokens。→ [第 02 章 · 第 4.3 节](02-消息状态与上下文窗口.md#43-为什么长上下文不是万能药)

- **Q：最简单的历史治理手段是什么？代价是什么？**
  - 答：截断——保留 system + 最近 N 条。极便宜，但截断点之前的记忆直接丢失。矛盾（成本 vs 记忆）推动升级：压缩/摘要（第 08 章）、外置记忆按需读取（第 09 章）。→ [第 02 章 · 第 5 节](02-消息状态与上下文窗口.md#5、历史治理第一刀截断)

### 第 05 章（循环层）

- **Q：讲一遍 Function Calling 的完整闭环。**
  - 答：三件事——① 声明工具 schema（name/description/参数 JSON Schema）；② 模型返回 `tool_calls`（注意 `arguments` 是字符串化 JSON，要自己解析）；③ 执行后以 `role="tool"` + `tool_call_id` 回喂结果。闭环完成，模型基于观察继续决策。→ [第 05 章 · 第 1 节](05-手写第一个工具循环.md#1、从聊天到行动缺的那一环)

- **Q：`tool_call_id` 是干嘛的？丢了会怎样？**
  - 答：把 `role="tool"` 的结果和 assistant 的某次工具请求对上号。丢了或对不上，API 直接报 `Invalid parameter: messages`。手工拼 tool 消息是高频翻车点，应封装进执行器。→ [第 05 章 · 第 6 节](05-手写第一个工具循环.md#6、常见报错排查)

- **Q：Agent 循环怎么终止？为什么必须有护栏？**
  - 答：两类——自然终止（模型不再要工具）和预算终止（轮数/token/超时撞上限）。护栏必须有，因为模型不存在「一定会停」的保证：工具报错它重试、结果不满意它再查。所有框架都有对应物（`max_turns` / `recursion_limit`）。→ [第 05 章 · 第 4 节](05-手写第一个工具循环.md#4、终止条件与护栏循环的方向盘)

- **Q：工具执行失败（参数非法/抛异常），循环应该怎么处理？**
  - 答：不炸循环——捕获异常，把错误文本作为 Observation 回喂给模型，让它自己纠错（改参数/换工具/放弃）。这是 Agent 可靠性的底线设计，也是第 06 章容错的起点。→ [第 05 章 · 第 3.1 节](05-手写第一个工具循环.md#31-设计要点)

- **Q：ReAct 和今天手写的工具循环是什么关系？**
  - 答：ReAct 是 2022 年的提示词模式（prompt 里要求输出 Thought:/Action:/Observation:）；Function Calling 原生化了这套结构。**循环是本体，ReAct 是理解它的教具**——能讲清这个演化的候选人比背定义的强。→ [第 05 章 · 第 4 节](05-手写第一个工具循环.md#4、终止条件与护栏循环的方向盘)

### 第 06 章（容错层）

- **Q：工具调用失败了，你会怎么处理？**
  - 答：分三层，从贵到便宜——① 回喂模型让它智能决策（贵，重发历史+重新生成）；② 工具边界内重试（便宜，只重发一次工具调用，瞬时抖动适用）；③ 降级/兜底默认值（最便宜，可能 0 次模型调用）。→ [第 06 章 · 第 4 节](06-工具集合与容错.md#4、工具失败的三种兜底串联-05-与-06)

- **Q：为什么不在循环层重试，而要在工具层重试？**
  - 答：模型重试一轮 = 重发全部历史 + 重新生成 token，代价高且不确定；工具层重试只重发一次工具调用。前提是「接口一致」：`ResilientTool` 与 `Tool` 同接口，`AgentLoop` 无感调用，所以容错能下沉到工具层而不改 `run()` 签名。→ [第 06 章 · 第 1 节](06-工具集合与容错.md#1、为什么工具边界要容错) + [第 3.2 节](06-工具集合与容错.md#32-resilienttool)

- **Q：什么情况下不能盲目重试？怎么解决？**
  - 答：工具**非幂等**时（下单/扣款），重试=重复副作用。两个解法：容错层显式把 `idempotent=False` 当作门槛拒绝自动重试；真要安全重试，靠调用方带**幂等键**（idempotency key），服务端同 key 去重返回第一次结果。→ [第 06 章 · 第 2.3 节](06-工具集合与容错.md#23-幂等idempotency)

- **Q：超时和取消有什么区别？**
  - 答：超时只是「不再等」；Python 线程模型下杀不掉已启动的线程，所以超时后线程可能仍在后台跑。生产环境要用异步/取消机制配合，否则对非幂等工具，超时后重试仍有重复副作用风险。→ [第 06 章 · 第 6 节](06-工具集合与容错.md#6、常见报错排查)

- **Q：重试的退避怎么设计？**
  - 答：指数退避 + 抖动（0.5s→1s→2s … + jitter），防止固定短间隔硬撞造成重试风暴；要有上限 `max_retries`，且只重试「可重试」的错误（网络/限流/瞬时锁），入参错重试一万遍也没用。→ [第 06 章 · 第 2.1 节](06-工具集合与容错.md#21-重试retry)

### 第 07 章（结构化输出层）

- **Q：怎么保证模型的输出「稳定可解析」？**
  - 答：三层叠加——① JSON Mode（`response_format={"type":"json_object"}`，保证合法 JSON）；② Pydantic 校验（类型/必填/约束，锁死字段）；③ 自纠重试（校验失败把错误回喂模型，让它自己改）。三层兜底把「靠正则抓字眼」变成「靠类型系统把关」。→ [第 07 章 · 第 2 节](07-结构化输出与Schema设计.md#2、三层武器json-mode-pydantic自纠)

- **Q：JSON Mode 和原生 Structured Output 的区别？**
  - 答：JSON Mode 只保证「是合法 JSON」，字段名/类型仍可能跑偏（要配 prompt 里的 schema 提示）；原生 `/json_schema` structured output 把 schema 直接交给 API 硬约束结构，但各提供商支持不一。工程上常在「跨提供商通用（JSON Mode+提示）」和「强约束（native）」之间权衡。→ [第 07 章 · 第 2.1 节](07-结构化输出与Schema设计.md#21-json-mode第一层保证是-json)

- **Q：模型输出带了 ```json 围栏或前后废话，怎么办？**
  - 答：`extract_json` 先剥围栏再取最外层花括号，跟着 `json.loads` 做严格解析——不靠正则硬凑，解析失败就抛错进入自纠。→ [第 07 章 · 第 3.1 节](07-结构化输出与Schema设计.md#31-extract-json抠出-json)

- **Q：schema 在「工具入参」和「模型输出」里是什么关系？**
  - 答：同一个思想的两面：第 05 章用 JSON Schema 约束**工具要的入参**，第 07 章用 Pydantic/JSON Schema 约束**模型给的输出**。锁住「模型说的」和「模型要的」，Agent 每一步才都能被程序可靠消费。→ [第 07 章 · 第 4 节](07-结构化输出与Schema设计.md#4、schema-的一鱼两吃入参与输出是同一个思想)

- **Q：结构化输出为什么对 Agent 尤其重要？**
  - 答：Agent 是「思考→决策→行动」的循环，每一步的中间产出要喂给程序（路由、工具、控制流）；自由文本不可靠、不可校验。把输出关进类型系统，下游才能不靠正则和祈祷地接住模型的概率性输出。→ [第 07 章 · 第 1 节](07-结构化输出与Schema设计.md#1、为什么模型输出需要结构)

### 第 08 章（Context 治理层）

- **Q：多轮对话越聊越长，你怎么控制上下文成本？**
  - 答：先分清「热而短反复用」和「大而冷偶尔用」——前者压进上下文，后者只存引用按需读。具体三招：`skip` 最旧的、`compact` 压成摘要、按 `budget` 取舍；再配 `estimate_tokens` 量得出预算，才有资格谈省。→ [第 08 章 · 第 2 节](08-Context治理.md#2、三条治理路线一张决策表)

- **Q：压缩上下文时，为什么最近几轮要原样保留而不是一起压掉？**
  - 答：最近几轮承载当前任务的直接脉络——刚问的、刚决定的，正是下一步要接着用的；而更早的部分语义可被摘要浓缩。`compact` 的黄金结构 = 摘要（旧历史）+ 最近 N 轮原文，用 5% 的 token 承载 80% 的语义，又不丢当下的连贯性。→ [第 08 章 · 第 3.3 节](08-Context治理.md#33-summarize--compact压缩)

- **Q：什么是 lost in the middle？对 Agent 设计有什么影响？**
  - 答：长上下文里模型对开头结尾记得牢、中段利用率断崖下降。影响：不是「塞得越多越好」，塞太多反而让中间糊掉、答偏。所以要克制地取舍——把关键信息放两端（system/最近），中段别堆无关内容。→ [第 08 章 · 第 1 节](08-Context治理.md#1、回到第-02-章那三根刺)

- **Q：RAG 和 Memory（记忆）的边界在哪？**
  - 答：本章点出雏形后第 09 章展开：Memory 是「关于这次任务/这个用户的长期状态」（要持续写、跨会话读）；RAG 是「按需检索外部知识」（只读、按 query 拉）。`ReferenceLibrary` 的「目录进上下文、需要时 fetch」更接近 RAG 的检索侧。→ [第 08 章 · 第 3.5 节](08-Context治理.md#35-referencelibrary按需读取)

- **Q：为什么「按条数」截断历史会有坑？**
  - 答：按条数裁奇数条可能从一轮中间切开、让历史以 `assistant` 开头，触发部分提供商的角色顺序报错。正确姿势是「按轮成对裁」（`keep_last_turns`），保证从 `user` 开始。→ [第 08 章 · 第 4 节](08-Context治理.md#4、还第-02-章的债truncate-按轮成对裁)

### 第 09 章（记忆体系）

- **Q：给 Agent 加「记忆」，你会分几层做？**
  - 答：三层——短期（会话内 `messages`）、长期（跨会话精确 key，`FileMemory`）、向量（跨会话按语义相似检索，`VectorMemory`）。短期管当下、长期/向量接过去。→ [第 09 章 · 第 1 节](09-记忆体系.md#1、记忆的三层短期--长期--向量)

- **Q：什么时候用文件记忆？什么时候用向量记忆？**
  - 答：判断标准是「精确 vs 模糊」——能 key 精确定位（用户名/偏好）用文件记忆，量小、读写都知道找什么；只能按意思找（「上次问过退款吗」这种没固定 key 的）用向量记忆。→ [第 09 章 · 第 2 节](09-记忆体系.md#2、什么时候用文件记忆什么时候用向量记忆)

- **Q：RAG 和 Memory 的边界在哪？**
  - 答：四个字「读 vs 写」。Memory 是**写**本体状态——关于这个用户/任务的事实、偏好，对话中持续写入、下次主动注入；RAG 是**读**外部知识——按 query 现查现用、不写回。记忆是自传，RAG 是图书馆。→ [第 09 章 · 第 3 节](09-记忆体系.md#3、rag-与-memory-的边界那道老题)

- **Q：向量检索的本质流程是什么？向量库在其中扮演什么角色？**
  - 答：三步——把文本编码成向量 → 算相似度（余弦）→ 取 top-k。向量库（Chroma/FAISS）是「量大到内存装不下时才需要的加速器」，**不是记忆本身**；语义来自 embedding 模型，不是向量库。→ [第 09 章 · 第 2 节](09-记忆体系.md#2、什么时候用文件记忆什么时候用向量记忆) + [第 4.2 节](09-记忆体系.md#42-vectormemory语义检索的机械结构)

- **Q：为什么玩具嵌入（字符 n-gram）抓不到「退钱/退款政策」这类同义？**
  - 答：玩具嵌入靠「字符共现」衡量**表面相似**，同义改写表面字符不同就散开了；真语义要拿上下文级语义的 embedding 模型（`toy_embed` 只是演示检索机械结构的教具，`embed=` 可替换）。→ [第 09 章 · 第 2 节](09-记忆体系.md#2、什么时候用文件记忆什么时候用向量记忆)

### 第 10 章（任务拆解与 Planning）

- **Q：ReAct 和 Plan-and-Execute 的本质区别是什么？如何取舍？**
  - 答：ReAct 把「计划」藏在每轮决策里（边想边做、每轮只定下一步），P&E 把「计划」提到台面上变成显式清单（先拆再执行）。取舍：计划越好 P&E 越省（每步不用重想全局），计划越可能错越该回 ReAct（随时纠偏）；现实常混用——先 P&E 出大纲、每步内再跑小 ReAct。→ [第 10 章 · 第 1 节](10-任务拆解与Planning.md#1、两种范式react-与-plan-and-execute)

- **Q：为什么要让「计划」变成一等对象（Plan/PlanStep），而不是写进 prompt 的散文？**
  - 答：三样 ReAct 给不了的——可审查（先看计划再执行）、可观察（progress() 一眼看到做到第几步）、可恢复（状态存着，断了接着跑，第 12 章 Checkpoint 的地基）。→ [第 10 章 · 第 2 节](10-任务拆解与Planning.md#2、为什么计划要变成一等对象)

- **Q：让 LLM 拆任务时，怎么保证它吐的是结构而不是散文？**
  - 答：复用结构化输出——`make_plan` 内部走 `structured_chat`，让模型输出 `{"steps": [...]}`，解析失败自动自纠，拿回 `list[str]` 而非靠正则硬抠。→ [第 10 章 · 第 3.2 节](10-任务拆解与Planning.md#32-make-plan复用自己的第-07-章)

- **Q：P&E 执行到某一步失败了怎么办？**
  - 答：本章先立「失败即停」——计划中途断开不该硬往下跑。补偿（重试/反思/重规划）是第 11 章失败策略的戏。`execute_plan` 每步先置 in_progress、失败置 failed 并停止。→ [第 10 章 · 第 3.3 节](10-任务拆解与Planning.md#33-execute-plan逐步执行--失败即停)

- **Q：P&E 的「计划」和 Memory 的「记忆」在 Agent 里是什么关系？**
  - 答：记忆是过去（第 09 章，沉淀已发生的事实/偏好），计划是未来（第 10 章，固化即将做的事）。两者接缝：长期记忆里的偏好/事实，正是 `make_plan` 拆任务的输入约束。→ [第 10 章 · 第 4 节](10-任务拆解与Planning.md#4、串联planning-与前面每一章)

### 第 11 章（失败策略与反思）

- **Q：工具调用失败了，什么时候「重试就可以」，什么时候「得让模型反思」？**
  - 答：分两类——瞬时故障（超时/限流/网络抖动）用傻重试（`RetryPolicy`，同动作退避重来，便宜快）；「动作/思路本身可能错了」用反思式重试（把失败回喂模型换思路，贵但在想）。判断标准：失败原因是不是「换时间就能消失」。→ [第 11 章 · 第 1 节](11-失败策略与反思.md#1、失败策略不单独成范式它服务谁)

- **Q：Self-Reflection 的本质是什么？**
  - 答：把失败信息回喂给模型，让它思考「为什么失败、该换什么」，输出 `verdict/reason/next_action` 三件套。和第 07 章「把解析错误回喂自纠」是同一招，只是粒度从格式升到任务。→ [第 11 章 · 第 2 节](11-失败策略与反思.md#2、self-reflection-的本质把失败喂回去)

- **Q：反思式重试和普通重试的分水岭在哪？**
  - 答：看它有没有产出「换个什么」——`next_action` 得给出具体改法；如果只吐「再试一次」，就退化成了傻重试。→ [第 11 章 · 第 2 节](11-失败策略与反思.md#2、self-reflection-的本质把失败喂回去)

- **Q：反思式重试会不会死循环？怎么防？**
  - 答：会，如果 `next_action` 每次换汤不换药。防法：给反思设上限（`max_reflections`，默认 2）、支持 `verdict=give_up` 主动放弃、并把每轮记录进 `attempts` 供排查。→ [第 11 章 · 第 3.2 节](11-失败策略与反思.md#32-retry-with-reflection反思式重试循环)

- **Q：为什么说失败策略「不单独成主线范式」？**
  - 答：它不改变「怎么干活」，只决定「干砸了怎么站起来」——是给 ReAct（05）和 Plan-and-Execute（10）兜底的配角：ReAct 某轮 tool 失败该不该反思再出招，P&E 某步 failed 停完该重做还是重规划。→ [第 11 章 · 第 1 节](11-失败策略与反思.md#1、失败策略不单独成范式它服务谁)

### 第 12 章（Checkpoint 与状态恢复）

- **Q：长任务跑一半进程崩了，怎么保证不从头重来？**
  - 答：状态序列化 + 断点续跑——把运行中的 Plan 每步状态（done/in_progress/failed）落盘成 checkpoint，进程崩了从盘上读回，重跑一切「没 done」的步骤。done 是唯一稳定边界。→ [第 12 章 · 第 1 节](12-Checkpoint与状态恢复.md#1、为什么状态要能落地)

- **Q：Durable Execution 的核心思想是什么？**
  - 答：三句话——状态是唯一事实（SSOT）、SAVE 是常态不是事后（每步前后都落盘）、执行只是推进状态。进程易失、文件可靠；崩溃后恢复 = 加载状态 + 继续推进。→ [第 12 章 · 第 1 节](12-Checkpoint与状态恢复.md#1、为什么状态要能落地)

- **Q：断点续跑时，`failed` 的步骤要不要重跑？为什么这可能和「失败即停」冲突？**
  - 答：要重跑（at-least-once）——「没 done 就是没做完」，宁可多做不可假装做过；这和第 10 章 execute_plan「失败即停、failed 永久跳过」是两种政策：前者为持久续航、后者为快速止损。代价是副作用可能重复，需让步骤尽量幂等。→ [第 12 章 · 第 2 节](12-Checkpoint与状态恢复.md#2、断点续跑-vs-从头再跑)

- **Q：at-least-once 有什么代价？怎么缓解？**
  - 答：某步有副作用（发邮件、扣款）时可能重复执行。缓解：落盘刷得勤（压小重复窗口）、把 done 的标记当去重边界、副作用步骤做成幂等。→ [第 12 章 · 第 2 节](12-Checkpoint与状态恢复.md#2、断点续跑-vs-从头再跑)

- **Q：Checkpoint（状态）和 Trace（过程）都落盘，区别在哪？**
  - 答：Checkpoint 为**恢复**（状态快照，能接着跑），Trace 为**排查/回放**（过程日志，能复盘哪里错）。一个是「现在到哪了」，一个是「这一路怎么走的」。→ [第 12 章 · 第 4 节](12-Checkpoint与状态恢复.md#4、串联checkpoint-与前几章)

### 第 13 章（封装 Mini Agent Runtime）

- **Q：什么是 Agent 状态机？为什么要显式建模状态？**
  - 答：显式标出 Agent「现在到哪了」（new→running→done/error），非法迁移直接报错。因为「机器能判定的状态别靠人眼看日志猜」——`agent.state==done` 是可编程判定的真话，「应该跑完了」是要人去猜的话。→ [第 13 章 · 第 2 节](13-封装MiniAgentRuntime.md#2、agent-状态机显式--有护栏)

- **Q：MiniAgent 的「事件循环」和「钩子」是什么关系？**
  - 答：一次 run 就是一圈状态机（start→…→finish/error），关键节点发 `RuntimeEvent`；钩子（on_event）是注册在事件上的观察者回调，让上层能观察每一步——这正是 trace 的原料。→ [第 13 章 · 第 3.1 节](13-封装MiniAgentRuntime.md#31-事件循环--钩子)

- **Q：手写的 MiniAgent 和 LangGraph / OpenAI Agents SDK 差在哪？**
  - 答：不是「我比框架强」，而是「价值观一致、量产细节不同」。框架同样信奉显式状态、可插拔、可观察，只是替你写好了更多生产细节（图可视化/checkpoint、handoff/guardrail/原生 tracing）。看懂 mini runtime，看框架就不再是黑魔法。→ [第 13 章 · 第 4 节](13-封装MiniAgentRuntime.md#4、对比langgraph--openai-agents-sdk)

- **Q：同一个 Agent 实例跑完一单，能直接跑第二单吗？**
  - 答：不能直接 `run`——状态机在 done/error 上会拒绝非法迁移；要 `reset()` 回到 new（清空本次 events）再跑。这是状态机的护栏。→ [第 13 章 · 第 2 节](13-封装MiniAgentRuntime.md#2、agent-状态机显式--有护栏)

- **Q：MiniAgent 事件里为什么看不到工具调用这一级？**
  - 答：因为 MiniAgent 委托 `AgentLoop` 干活，细粒度（每轮、每次工具调用）没暴露。要在循环里打更细的桩——这正是第 14 章 trace 要补的。→ [第 13 章 · 第 5 节](13-封装MiniAgentRuntime.md#5、运行本章案例)

### 第 14 章（可观测：Trace / 日志 / 回放）

- **Q：log / metric / trace / replay 四者分工是什么？**
  - 答：log 是原料（一条条记录）、metric 是总结（聚合数字判健康）、trace 是叙事（按顺序串成时间线）、replay 是回放（用录下的内容重跑复现）。原料记全了，后三样都能从它派生。→ [第 14 章 · 第 1 节](14-可观测Trace日志回放.md#1、log--metric--trace--replay-四件套)

- **Q：AgentLoop 是个黑盒，怎么不侵入地拿到每一步的观测？**
  - 答：两个 hook 都不改循环——包装 llm（Tracer(wrap=llm) 在每次 chat 前后记事件，捕获「每轮 + 每轮要了几次工具」）+ 挂 MiniAgent 钩子（收 start/finish/error 状态）。能力走包装、不走侵入。→ [第 14 章 · 第 2 节](14-可观测Trace日志回放.md#2、不侵入-agentloop怎么把每一步记下来)

- **Q：Agent 的 trace 和传统后端的 trace 有什么不同？**
  - 答：核心还是 span 化记录，只是「一个 span」常对应「一次 LLM 调用/一次工具调用」而非一次 RPC；且 agent trace 要额外连上「状态」（run.start/finish/error、final_state），因为 agent 的价值不只在耗时，还在「走到哪、停没停对」。→ [第 14 章 · 第 4 节](14-可观测Trace日志回放.md#4、接入-langfuse或其他观测平台)

- **Q：想做离线回放复现 bug，关键是什么？**
  - 答：把「模型这一步的产出」（LLMResult 序列）录下来，喂给 `ScriptedLLM` 重跑同样输入和工具即可离线复现——真实回放还要连输入、工具结果一起录（第 17 章完整做）。→ [第 14 章 · 第 3 节](14-可观测Trace日志回放.md#3、内核harnesstracepy第十二块砖)

- **Q：接入 Langfuse 该从哪下手？**
  - 答：分清「记什么」和「记到哪」——我们 Tracer 的 events 已经把「该记什么」立住了（start/finish/llm.call/llm.return/tool_calls 计数），接 Langfuse 只是把这些事件映射成它的 span（generation/trace）。hook 已在，剩下是换 callback。→ [第 14 章 · 第 4 节](14-可观测Trace日志回放.md#4、接入-langfuse或其他观测平台)

### 第 15 章（终止条件 · 权限 · 安全）

- **Q：生产级 Agent 的「安全三件事」是什么？**
  - 答：该停停得住（终止条件：max_rounds/输出预算/停止短语）、不该碰碰不了（最小授权 + 人工审批）、坏东西跑不出圈（sandbox）。再加一件事——Prompt Injection：别把不可信内容当指令。→ [第 15 章 · 第 1 节](15-终止条件权限安全.md#1、安全三件事--一件事)

- **Q：为什么 Agent 权限要在「工具层」做，而不是在循环里？**
  - 答：因为工具调用面就是攻击面，而且工具层可复用包装、不侵入循环——`PolicyGuard.wrap(Tool)` 套上鉴权/审批/sandbox，AgentLoop/MiniAgent 零改动；拦截抛出的 ToolError 还会被回喂给模型，让模型自己转弯。→ [第 15 章 · 第 2 节](15-终止条件权限安全.md#2、最小授权--审批--sandbox三件套)

- **Q：最小授权的「read / write / execute」三级在拦什么？**
  - 答：给 Agent 的授权定一个水位，工具声明所需权限；`granted < required` 直接拒。比如只读场景授权 read，模型就算被诱导「删库」也调不动 write/execute 工具——权限是兜底，不赌模型「知道分寸」。→ [第 15 章 · 第 1 节](15-终止条件权限安全.md#1、安全三件事--一件事)

- **Q：黑名单 sandbox 够不够？为什么？**
  - 答：不够——黑名单拦得住「图省事/误伤」的调用（rm -rf、读密码、起子进程），拦不住「蓄意绕过」；真对抗要系统级隔离（子进程限额、容器、只读挂载、网络 allowlist）。黑名单是第一道不是唯一一道。→ [第 15 章 · 第 2 节](15-终止条件权限安全.md#2、最小授权--审批--sandbox三件套)

- **Q：Prompt Injection 的根治法是什么？**
  - 答：不是「识别所有注入话术」（启发式总会被绕过），而是**结构性隔离**：数据就是数据、指令可靠地来自系统/用户通道，不把不可信内容当指令执行；`detect_injection` 只是第一层嗅探。→ [第 15 章 · 第 1 节](15-终止条件权限安全.md#1、安全三件事--一件事)

### 第 16 章（MCP / A2A / Skills 接入）

- **Q：MCP 到底解决了什么问题？**
  - 答：把「谁定义工具」和「谁使用工具」解耦——Server 侧只管列出有什么工具（tools/list）、能调什么（tools/call），Client 侧（Agent）只消费，两者可跨进程/跨机器。互相升级、互不侵入，工具生态才转得起来。→ [第 16 章 · 第 2 节](16-MCP-A2A-Skills接入.md#2、mcp工具谁定义与谁消费解耦)

- **Q：为什么 MCP 工具能零改动接进 MiniAgent？**
  - 答：因为协议最终都落到「Tool」这个统一接缝——`MCPClient.to_harness_tools()` 把远程 MCP 工具适配成本框架的 Tool，MiniAgent 只认 Tool、不认「本地还是远程」。位置透明，是适配器的功劳。→ [第 16 章 · 第 2 节](16-MCP-A2A-Skills接入.md#2、mcp工具谁定义与谁消费解耦)

- **Q：A2A 和 MCP 的区别？什么时候用哪个？**
  - 答：MCP 接「工具/服务」（无状态、一次调用一结果），A2A 接「别的 Agent」（有状态、能自主推理决策）。要查天气走 MCP，要「找个专家帮我判断一下」走 A2A。它们不冲突，常一起用。→ [第 16 章 · 第 3 节](16-MCP-A2A-Skills接入.md#3、a2a专家也是一种工具)

- **Q：Skills 相比「把所有工具一次性塞进 system prompt」好在哪里？**
  - 答：按需挂载——`catalog()` 先让模型知道有哪些技能，`activate()` 只挂当前要用的（prompt + 工具），省 token、减少工具间干扰、也便于把「怎么做一件事」沉淀成可复用资产。→ [第 16 章 · 第 4 节](16-MCP-A2A-Skills接入.md#4、skills按需挂载别一次塞满)

- **Q：外部协议接入的工具，怎么复用前面章节的容错与权限？**
  - 答：协议只是「通道」，适配成 Tool 后就是普通能力积木——照常套 `ResilientTool`（第 06 章容错）和 `PolicyGuard.wrap(tool, ToolPolicy(...))`（第 15 章权限）。协议负责「接得进来」，能力积木负责「接得住」。→ [第 16 章 · 第 6 节](16-MCP-A2A-Skills接入.md#6、串联协议与前面每一章)

---

## 一、LLM 基础

- Transformer 与 RNN/LSTM 的区别？为什么生成式产品偏爱 decoder-only？
- 采样参数（temperature/top-p）控制什么？为什么不能靠它替代任务建模？
- KV Cache 是什么？Prefix Caching 和 Prompt Caching 的区别？
- 长上下文为什么不一定真的有用？常见优化思路？
- 幻觉的根因？怎么分层降低？

## 二、Prompt 与 Context

- Prompt Engineering 的核心原则？什么是好的 prompt？
- Context Engineering 和 Prompt Engineering 的关系与区别？
- Function Calling 的本质流程？什么是好的 Tool Schema？
- 结构化输出为什么重要？怎么保证稳定？

## 三、Agent 核心范式

- Agent 和 Workflow 的边界？什么时候该用 Agent？
- ReAct、Plan-and-Execute、CodeAct 各自适用场景？
- 多 Agent 架构什么时候有价值，什么时候是过度设计？
- Agent 状态机与终止条件怎么设计？
- 你怎么判断「模型更强后，哪些脚手架该删、哪些该加强」？

## 四、RAG 与记忆

- Chunking/Embedding/Rerank 怎么做？
- Hybrid Retrieval 与 Query Rewrite？
- RAG 和 Memory 的边界？
- 长期记忆与用户记忆怎么设计？
- Agentic RAG / GraphRAG 是什么？

## 五、协议、Runtime 与工程化

- MCP 解决什么问题？和 Function Calling 的关系？
- A2A 与 Agent 互操作？
- Session-State 与 Context 生命周期怎么管理？
- 任务调度、重试、幂等、回滚怎么做？
- **Agent Runtime 设计（重中之重）**：你怎么设计一个 Agent Runtime？
- Skills、插件化与能力封装的意义？

## 六、评测与工程化

- Agent 评测比普通 LLM 评测难在哪？维度有哪些？
- Offline-Eval 与 Online-Eval 的分工？
- 过程指标与结果指标怎么配对？
- 可观测：log/metric/trace/replay 各负责什么？为什么没有 trace 就没有真正的调试？
- Prompt Injection 与工具安全怎么防？
- 成本/延迟/稳定性怎么优化？
- 灰度发布、版本管理、回滚？

## 七、前沿主线

- 「从 Prompt 到 Harness」这条主线在讲什么？
- Anthropic / OpenAI 近一年在 agent 方向上的工程化路线？
- 代码 Agent（Claude Code / Codex / OpenCode）为什么是 Agent 工程试验场？
