# 面试题库

本题库对标 2026 「AI Agent / 大模型应用开发工程师」岗位高频考点，按能力域组织「问法 + 答法要点」。题目来源覆盖大厂真实面经与高频追问，并与正文章节互链。

> 答题核心主线：**工程重心已从 Prompt 迁移到 context、harness、eval、protocol、runtime。** 讲项目时少说「我用了 ReAct/RAG/LangChain」，多说「我怎么治理上下文、怎么设计工具 schema、怎么做 checkpoint 和 trace、怎么定终止条件和权限」。

---

## 〇、已发布章节可覆盖的题（带答法要点）

### 第 01 章（调用层）

- **Q：接入一个新的大模型，最少要准备哪几样东西？**
  - 答：**调用三件套**——API Key、模型名、Base URL。只要是兼容 OpenAI 协议的提供商（DeepSeek/通义/OpenAI/Ollama），同一套 SDK 代码只换这三个值就能跑。→ [第 01 章 · 第 2 节](01-LLM调用与环境准备.md#2、调用三件套api-key模型名base-url)

- **Q：同步调用和流式调用怎么选？流式调用有什么坑？**
  - 答：面向用户的对话/Agent 中间步骤用流式，后台批处理用同步。流式首块 `delta.content` 可能为 `None`，要 `or ""` 兜底。→ [第 01 章 · 第 4 节](01-LLM调用与环境准备.md#4、第一次调用同步-流式)

- **Q：为什么这么多国产模型都能用 OpenAI SDK 调？**
  - 答：OpenAI 的 Python SDK 成了事实标准，DeepSeek、通义、Ollama 主动兼容它，所以换 provider 只是换三个配置值，不动业务代码。→ [第 01 章 · 第 2.2 节](01-LLM调用与环境准备.md#22-为什么是同一套代码)

- **Q：什么是 Agent Harness？为什么 2026 这么强调它？**
  - 答：Harness 是包裹 LLM 的运行时脚手架——状态、工具、checkpoint、trace、权限、终止条件。重心从 Prompt 迁到 harness，是因为模型更强后，长任务的可靠性、可恢复、可审计成了真问题。→ [第 01 章 · 第 1 节](01-LLM调用与环境准备.md#1、为什么这份教程不用现成框架)

- **Q：为什么这个教程不用 LangChain，而是从零手写？**
  - 答：不是否定框架，而是「亲手实现过 Harness，才能看懂框架替你做了什么」。本教程把 `LLMClient` 这层抽象拆开，对应 `ChatOpenAI` / `model=`，看清那 10 行，再用任何框架不发懵。→ [第 01 章 · 第 5 节](01-LLM调用与环境准备.md#5、封装-llmclient第一块-harness-砖)

### 第 02 章（状态层）

- **Q：大模型 API 是有状态还是无状态？多轮对话的「记忆」从哪来？**
  - 答：**无状态**。服务端不保存对话，每轮的「记忆」全靠客户端把全部历史消息重发一遍——所以消息列表就是 Agent 的第一个状态，管理它是工程问题不是模型问题。→ [第 02 章 · 第 1 节](02-消息状态与上下文窗口.md#1、消息列表就是-agent-的第一个状态)

- **Q：多轮对话为什么越聊越贵？成本怎么控制？**
  - 答：每轮 prompt_tokens ≈ 前面所有轮的总和，成本滚雪球。控制手段从上下文入手：截断、压缩摘要、外置记忆按需读取——这是 Context Engineering 的经济动因。→ [第 02 章 · 第 3 节](02-消息状态与上下文窗口.md#3、token-与成本多轮对话的隐藏账单)

- **Q：KV Cache / Prefix Caching 是什么？对写 Agent 有什么实际影响？**
  - 答：推理侧缓存「已算过的前缀」，命中则更快更便宜（部分提供商打折）。工程守则：**稳定内容放前面**（system、工具定义），多变内容放后面，提高前缀命中率。→ [第 02 章 · 第 4.2 节](02-消息状态与上下文窗口.md#42-kv-cache为什么前缀稳定能省钱提速)

- **Q：窗口都 128K 了，为什么还要做上下文治理？**
  - 答：三个坑——注意力退化（lost in the middle，塞越多中段利用率越低）、成本线性涨、首 token 延迟涨。窗口大只是「付得起」，不是「免单」，正确姿势是每轮只给最有用的 tokens。→ [第 02 章 · 第 4.3 节](02-消息状态与上下文窗口.md#43-为什么长上下文不是万能药)

- **Q：最简单的历史治理手段是什么？代价是什么？**
  - 答：截断——保留 system + 最近 N 条。极便宜，但截断点之前的记忆直接丢失。矛盾（成本 vs 记忆）推动升级：压缩/摘要（第 08 章）、外置记忆按需读取（第 09 章）。→ [第 02 章 · 第 5 节](02-消息状态与上下文窗口.md#5、历史治理第一刀截断)

### 第 05 章（循环层）

- **Q：讲一遍 Function Calling 的完整闭环。**
  - 答：三件事——① 声明工具 schema（name/description/参数 JSON Schema）；② 模型返回 `tool_calls`（注意 `arguments` 是字符串化 JSON，要自己解析）；③ 执行后以 `role="tool"` + `tool_call_id` 回喂结果。闭环完成，模型基于观察继续决策。→ [第 05 章 · 第 1 节](05-手写第一个工具循环.md#1、从聊天到行动缺的那一环)

- **Q：`tool_call_id` 是干嘛的？丢了会怎样？**
  - 答：把 `role="tool"` 的结果和 assistant 的某次工具请求对上号。丢了或对不上，API 直接报 `Invalid parameter: messages`。手工拼 tool 消息是高频翻车点，应封装进执行器。→ [第 05 章 · 第 6 节](05-手写第一个工具循环.md#6、常见报错排查)

- **Q：Agent 循环怎么终止？为什么必须有护栏？**
  - 答：两类——自然终止（模型不再要工具）和预算终止（轮数/token/超时撞上限）。护栏必须有，因为模型不存在「一定会停」的保证：工具报错它重试、结果不满意它再查。所有框架都有对应物（`max_turns` / `recursion_limit`）。→ [第 05 章 · 第 4 节](05-手写第一个工具循环.md#4、终止条件与护栏循环的方向盘)

- **Q：工具执行失败（参数非法/抛异常），循环应该怎么处理？**
  - 答：不炸循环——捕获异常，把错误文本作为 Observation 回喂给模型，让它自己纠错（改参数/换工具/放弃）。这是 Agent 可靠性的底线设计，也是第 06 章容错的起点。→ [第 05 章 · 第 3.1 节](05-手写第一个工具循环.md#31-设计要点)

- **Q：ReAct 和今天手写的工具循环是什么关系？**
  - 答：ReAct 是 2022 年的提示词模式（prompt 里要求输出 Thought:/Action:/Observation:）；Function Calling 原生化了这套结构。**循环是本体，ReAct 是理解它的教具**——能讲清这个演化的候选人比背定义的强。→ [第 05 章 · 第 4 节](05-手写第一个工具循环.md#4、终止条件与护栏循环的方向盘)

### 第 06 章（容错层）

- **Q：工具调用失败了，你会怎么处理？**
  - 答：分三层，从贵到便宜——① 回喂模型让它智能决策（贵，重发历史+重新生成）；② 工具边界内重试（便宜，只重发一次工具调用，瞬时抖动适用）；③ 降级/兜底默认值（最便宜，可能 0 次模型调用）。→ [第 06 章 · 第 4 节](06-工具集合与容错.md#4、工具失败的三种兜底串联-05-与-06)

- **Q：为什么不在循环层重试，而要在工具层重试？**
  - 答：模型重试一轮 = 重发全部历史 + 重新生成 token，代价高且不确定；工具层重试只重发一次工具调用。前提是「接口一致」：`ResilientTool` 与 `Tool` 同接口，`AgentLoop` 无感调用，所以容错能下沉到工具层而不改 `run()` 签名。→ [第 06 章 · 第 1 节](06-工具集合与容错.md#1、为什么工具边界要容错) + [第 3.2 节](06-工具集合与容错.md#32-resilienttool)

- **Q：什么情况下不能盲目重试？怎么解决？**
  - 答：工具**非幂等**时（下单/扣款），重试=重复副作用。两个解法：容错层显式把 `idempotent=False` 当作门槛拒绝自动重试；真要安全重试，靠调用方带**幂等键**（idempotency key），服务端同 key 去重返回第一次结果。→ [第 06 章 · 第 2.3 节](06-工具集合与容错.md#23-幂等idempotency)

- **Q：超时和取消有什么区别？**
  - 答：超时只是「不再等」；Python 线程模型下杀不掉已启动的线程，所以超时后线程可能仍在后台跑。生产环境要用异步/取消机制配合，否则对非幂等工具，超时后重试仍有重复副作用风险。→ [第 06 章 · 第 6 节](06-工具集合与容错.md#6、常见报错排查)

- **Q：重试的退避怎么设计？**
  - 答：指数退避 + 抖动（0.5s→1s→2s … + jitter），防止固定短间隔硬撞造成重试风暴；要有上限 `max_retries`，且只重试「可重试」的错误（网络/限流/瞬时锁），入参错重试一万遍也没用。→ [第 06 章 · 第 2.1 节](06-工具集合与容错.md#21-重试retry)

### 第 07 章（结构化输出层）

- **Q：怎么保证模型的输出「稳定可解析」？**
  - 答：三层叠加——① JSON Mode（`response_format={"type":"json_object"}`，保证合法 JSON）；② Pydantic 校验（类型/必填/约束，锁死字段）；③ 自纠重试（校验失败把错误回喂模型，让它自己改）。三层兜底把「靠正则抓字眼」变成「靠类型系统把关」。→ [第 07 章 · 第 2 节](07-结构化输出与Schema设计.md#2、三层武器json-mode-pydantic自纠)

- **Q：JSON Mode 和原生 Structured Output 的区别？**
  - 答：JSON Mode 只保证「是合法 JSON」，字段名/类型仍可能跑偏（要配 prompt 里的 schema 提示）；原生 `/json_schema` structured output 把 schema 直接交给 API 硬约束结构，但各提供商支持不一。工程上常在「跨提供商通用（JSON Mode+提示）」和「强约束（native）」之间权衡。→ [第 07 章 · 第 2.1 节](07-结构化输出与Schema设计.md#21-json-mode第一层保证是-json)

- **Q：模型输出带了 ```json 围栏或前后废话，怎么办？**
  - 答：`extract_json` 先剥围栏再取最外层花括号，跟着 `json.loads` 做严格解析——不靠正则硬凑，解析失败就抛错进入自纠。→ [第 07 章 · 第 3.1 节](07-结构化输出与Schema设计.md#31-extract-json抠出-json)

- **Q：schema 在「工具入参」和「模型输出」里是什么关系？**
  - 答：同一个思想的两面：第 05 章用 JSON Schema 约束**工具要的入参**，第 07 章用 Pydantic/JSON Schema 约束**模型给的输出**。锁住「模型说的」和「模型要的」，Agent 每一步才都能被程序可靠消费。→ [第 07 章 · 第 4 节](07-结构化输出与Schema设计.md#4、schema-的一鱼两吃入参与输出是同一个思想)

- **Q：结构化输出为什么对 Agent 尤其重要？**
  - 答：Agent 是「思考→决策→行动」的循环，每一步的中间产出要喂给程序（路由、工具、控制流）；自由文本不可靠、不可校验。把输出关进类型系统，下游才能不靠正则和祈祷地接住模型的概率性输出。→ [第 07 章 · 第 1 节](07-结构化输出与Schema设计.md#1、为什么模型输出需要结构)

### 第 08 章（Context 治理层）

- **Q：多轮对话越聊越长，你怎么控制上下文成本？**
  - 答：先分清「热而短反复用」和「大而冷偶尔用」——前者压进上下文，后者只存引用按需读。具体三招：`skip` 最旧的、`compact` 压成摘要、按 `budget` 取舍；再配 `estimate_tokens` 量得出预算，才有资格谈省。→ [第 08 章 · 第 2 节](08-Context治理.md#2、三条治理路线一张决策表)

- **Q：压缩上下文时，为什么最近几轮要原样保留而不是一起压掉？**
  - 答：最近几轮承载当前任务的直接脉络——刚问的、刚决定的，正是下一步要接着用的；而更早的部分语义可被摘要浓缩。`compact` 的黄金结构 = 摘要（旧历史）+ 最近 N 轮原文，用 5% 的 token 承载 80% 的语义，又不丢当下的连贯性。→ [第 08 章 · 第 3.3 节](08-Context治理.md#33-summarize--compact压缩)

- **Q：什么是 lost in the middle？对 Agent 设计有什么影响？**
  - 答：长上下文里模型对开头结尾记得牢、中段利用率断崖下降。影响：不是「塞得越多越好」，塞太多反而让中间糊掉、答偏。所以要克制地取舍——把关键信息放两端（system/最近），中段别堆无关内容。→ [第 08 章 · 第 1 节](08-Context治理.md#1、回到第-02-章那三根刺)

- **Q：RAG 和 Memory（记忆）的边界在哪？**
  - 答：本章点出雏形后第 09 章展开：Memory 是「关于这次任务/这个用户的长期状态」（要持续写、跨会话读）；RAG 是「按需检索外部知识」（只读、按 query 拉）。`ReferenceLibrary` 的「目录进上下文、需要时 fetch」更接近 RAG 的检索侧。→ [第 08 章 · 第 3.5 节](08-Context治理.md#35-referencelibrary按需读取)

- **Q：为什么「按条数」截断历史会有坑？**
  - 答：按条数裁奇数条可能从一轮中间切开、让历史以 `assistant` 开头，触发部分提供商的角色顺序报错。正确姿势是「按轮成对裁」（`keep_last_turns`），保证从 `user` 开始。→ [第 08 章 · 第 4 节](08-Context治理.md#4、还第-02-章的债truncate-按轮成对裁)

---

## 一、LLM 基础

- Transformer 与 RNN/LSTM 的区别？为什么生成式产品偏爱 decoder-only？
- 采样参数（temperature/top-p）控制什么？为什么不能靠它替代任务建模？
- KV Cache 是什么？Prefix Caching 和 Prompt Caching 的区别？
- 长上下文为什么不一定真的有用？常见优化思路？
- 幻觉的根因？怎么分层降低？

## 二、Prompt 与 Context

- Prompt Engineering 的核心原则？什么是好的 prompt？
- Context Engineering 和 Prompt Engineering 的关系与区别？
- Function Calling 的本质流程？什么是好的 Tool Schema？
- 结构化输出为什么重要？怎么保证稳定？

## 三、Agent 核心范式

- Agent 和 Workflow 的边界？什么时候该用 Agent？
- ReAct、Plan-and-Execute、CodeAct 各自适用场景？
- 多 Agent 架构什么时候有价值，什么时候是过度设计？
- Agent 状态机与终止条件怎么设计？
- 你怎么判断「模型更强后，哪些脚手架该删、哪些该加强」？

## 四、RAG 与记忆

- Chunking/Embedding/Rerank 怎么做？
- Hybrid Retrieval 与 Query Rewrite？
- RAG 和 Memory 的边界？
- 长期记忆与用户记忆怎么设计？
- Agentic RAG / GraphRAG 是什么？

## 五、协议、Runtime 与工程化

- MCP 解决什么问题？和 Function Calling 的关系？
- A2A 与 Agent 互操作？
- Session-State 与 Context 生命周期怎么管理？
- 任务调度、重试、幂等、回滚怎么做？
- **Agent Runtime 设计（重中之重）**：你怎么设计一个 Agent Runtime？
- Skills、插件化与能力封装的意义？

## 六、评测与工程化

- Agent 评测比普通 LLM 评测难在哪？维度有哪些？
- Offline-Eval 与 Online-Eval 的分工？
- 过程指标与结果指标怎么配对？
- 可观测：log/metric/trace/replay 各负责什么？为什么没有 trace 就没有真正的调试？
- Prompt Injection 与工具安全怎么防？
- 成本/延迟/稳定性怎么优化？
- 灰度发布、版本管理、回滚？

## 七、前沿主线

- 「从 Prompt 到 Harness」这条主线在讲什么？
- Anthropic / OpenAI 近一年在 agent 方向上的工程化路线？
- 代码 Agent（Claude Code / Codex / OpenCode）为什么是 Agent 工程试验场？
