# 09 - 记忆体系

> 📌 **第 9 章 · 阶段三 Context 层（收官）** · [← 返回目录大纲](教程目录大纲.md) · [上一章 08 Context 治理 →](08-Context治理.md) · 下一章 10 任务拆解与 Planning（规划中）

---

**本章课程目标：**

- 把「记忆」这个被说滥的词拆成三层：**短期（会话内）/ 长期（跨会话，文件）/ 向量（按语义检索）**，并说清各自对应内核里的哪块。
- 掌握一个决策判断题：**什么时候用文件记忆（精确 key）、什么时候用向量记忆（模糊按意思找）**——以及「向量库作教具」的真实含义。
- 回答那道老题：**RAG 与 Memory 的边界到底在哪**。
- 落地第七块砖：`harness/memory.py`（`FileMemory` / `VectorMemory` / `toy_embed` / `retrieved_context`）。

**学习建议：** 本章全部 demo **不需要 API Key**——记忆是确定性的，「玩具嵌入」离线可跑，真实 embedding 只是把 `toy_embed` 换掉、检索流程不变。先把「检索机制」和「语义」这两件事分开看，才不会一上来就以为「向量库 = 记忆」。

---

## 1、记忆的三层：短期 / 长期 / 向量

「给 Agent 加记忆」说得很多，但「记忆」其实至少有三种，混在一起就容易瞎设计：

| 层 | 是什么 | 生命周期 | 内核里的对应 | 什么时候用 |
| --- | --- | --- | --- | --- |
| **短期** | 本次会话的历史 | 会话内 | `ChatSession.messages`（第 02 章） | 正在聊的上下文 |
| **长期** | 跨会话的「事实 / 偏好」 | 跨会话 | `FileMemory`（本章） | 精确能定位：用户名、偏好、目标 |
| **向量** | 按「语义相似」检索的片段 | 跨会话 | `VectorMemory`（本章） | 模糊按意思找：「上次问过退款吗」 |

第 08 章治理的那一大坨 `messages`，本质就是在管**短期记忆**（`compact` 压缩、`select_by_budget` 取舍）；而本章要解决的是**会话结束之后**——记忆如何活下来、如何被下一次会话用到。

> 一句话先立住：**短期记忆是「当下」的，长期/向量记忆是「过去」的。** 治理（08）管当下，记忆（09）接过去。

---

## 2、什么时候用文件记忆、什么时候用向量记忆

这也是一个判断题，抓住「精确 vs 模糊」就够：

**用文件记忆（`FileMemory`），当记忆能被一个 key 精确定位：**
- 「用户叫什么」→ `recall("user_name")`
- 「用户偏好住哪」→ `recall("prefers_hotel_area")`
- 量小、每条都重要、读写都是「知道要找什么」

**用向量记忆（`VectorMemory`），当记忆只能按意思找：**
- 「用户以前是不是问过退款怎么退？」——没有哪个 key 能精确命中这个问法
- 量大、条目多、查询是用自然语言「找相近的」

| | 文件记忆 | 向量记忆 |
| --- | --- | --- |
| 定位方式 | key 精确匹配 | 相似度 top-k |
| 适合 | 小、精确、条条重要 | 大、模糊、按意找 |
| 丢不丢 | 不丢，但要记得 key | 可能漏（依赖相似度） |
| 成本 | 极低 | 要 embedding（真语义时） |

> **「向量库」本身不是记忆，只是记忆的一种检索方式。** 本章 `VectorMemory` 用的 `toy_embed`（字符 2-gram 词袋）是**教具**——它离线可跑、能演示「编码 → 余弦相似 → top-k」这条流水线；真实项目把这行 `embed=` 换成 embedding 模型（OpenAI / DeepSeek 的 `text-embedding`），检索流程一个字不用改。真正的向量库（Chroma / FAISS / pgvector）是「量大到内存装不下时才需要的加速器」，见[工具导航](工具导航与参考资料索引.md)的附录条目。

---

## 3、RAG 与 Memory 的边界（那道老题）

面试常问，答法就四个字：**读 vs 写**。

| | Memory（记忆） | RAG（检索增强） |
| --- | --- | --- |
| 本质 | **写**本体的状态 | **读**外部知识 |
| 数据 | 关于「这个用户/这次任务」的持续状态 | 世界知识 / 文档 / 手册 |
| 方向 | 对话中**持续写入**，下次**主动注入** | 按 query **现查现用**，不写回 |
| 时态 | 过去对话的沉淀 | 当下的取材 |

两者的**接缝**在第 08 章的 `ReferenceLibrary` 已经露头：`retrieved_context`（本章）演示的就是 RAG 那半边——「检索 → 把命中的片段拼进 prompt」。而 `FileMemory.remember()` 演示的是 Memory 那半边——「把一件事记下来，下次主动给你」。

> 一句话收口：**Memory 是 Agent 的自传，RAG 是 Agent 的图书馆。** 自传越写越厚（要治理），图书馆现查不用背（要检索）。

---

## 4、内核：harness/memory.py（第七块砖）

### 4.1 FileMemory —— 跨会话的精确记忆

```python
mem = FileMemory("user_memory.json")
mem.remember("user_name", "小明")
mem.save()                       # 落盘，才算跨会话
# —— 另一个会话 / 进程 ——
mem2 = FileMemory("user_memory.json")
mem2.recall("user_name")         # '小明'
```

关键在 `save()`：**不落盘，进程一退记忆就没了**。这就是「短期 vs 长期」最硬的那条分界线。

### 4.2 VectorMemory —— 语义检索的机械结构

```python
vmem = VectorMemory()            # 默认 toy_embed 教具
vmem.add("用户投诉过服务太慢")
vmem.search("用户投诉服务太慢", top_k=3)
# → [{score: 0.80, text: "用户投诉过服务太慢", meta: {}}, ...]
```

拆开看就是三步：**`toy_embed(text)` 编码 → `cosine(a, b)` 比相似 → `top_k` 取前几**。这三步就是所有向量检索的骨架，无论玩具还是真 embedding 都一样。注意 `toy_embed` 抓的是**字面**相似（字符共现），不是语义——下一节案例会专门演示这个局限。

### 4.3 retrieved_context —— RAG 的最小闭环

```python
ctx = retrieved_context(vmem, "我想退款要怎么办", top_k=1)
prompt = {"role": "user", "content": ctx + "\n\n请问：退款的步骤是什么？"}
```

检索命中的记忆片段被拼成上下文，塞进 prompt——这是「把过去拉回来服务当下」的最小实现。完整实现见 [harness/memory.py](harness/memory.py)。

> 🧱 第七块砖落位，**阶段三 Context 层收官**：`context`（治理）+ `memory`（记忆）把模型的「工作内存」从「聊完就忘」变成了「记得住、想得起、查得到」。下一步进入阶段四 State 层——让长任务（多步计划、中间失败也能活下来）有了可靠地基。

---

## 5、运行本章案例

全部 demo 无需 `.env`：

```bash
python examples/09_memory.py
```

预期输出（全部确定性）：

```
① 三层记忆：短期 / 长期 / 向量
  短期记忆　= ChatSession.messages（本会话）→ 2 条
  长期记忆　= FileMemory（跨会话，key→value）
  向量记忆　= VectorMemory（语义相似检索）

② FileMemory：落盘，另一个会话才记得
  会话 B 取回 user_name：小明
  会话 B 取回 prefers：大阪在心斋桥附近定住宿
  会话 B 全部事实：{...}

③ VectorMemory：按『相似度』找，而不是按 key 精确找
  score=0.5477  用户投诉过服务太慢
  score=0.0861  用户偏好住心斋桥附近
  score=0.0716  用户想订东京到大阪的新干线票

④ 玩具嵌入的局限：语义等价但字面不同就抓不到（为何要真 embedding）
  句意都是「能不能退钱」，玩具嵌入只靠『用户』二字给到 0.1195
  而字面几乎逐字相同 → 相似度 0.8018

⑤ RAG 最小闭环：检索命中的记忆 → 拼进上下文
  拼进 prompt 的内容：
    以下是与你问题相关的记忆片段：
    - 退款需在 7 天内申请，附订单号
```

> 看 demo③④ 时抓住一个点：玩具嵌入靠**字符共现**，字面相近分数高（demo③ 的 0.55、demo④ 后句的 0.80）；但**语义等价、字面不同**就抓不到（demo④ 前句——句意都是「能不能退钱」，玩具嵌入只靠「用户」二字给到 0.12）。这正是真实项目为什么要换真 embedding、也是「向量库作教具」这句话的完整含义。

---

## 6、常见报错排查

| 报错 / 现象 | 原因 | 解决 |
| --- | --- | --- |
| 下次会话 `recall()` 取不到 | 只 `remember()` 没 `save()` | 短期 vs 长期的分界线就是落盘；记得 `save()` |
| 语义检索结果离谱 | 用了默认 `toy_embed`（表面相似） | 换成真实 embedding 模型：`VectorMemory(embed=your_embed)` |
| 检索太慢 / 内存爆 | 向量全量在内存、线性扫描 | 量大再上真向量库（Chroma/FAISS），见附录 |
| 分不清该存文件还是向量 | 没想「精确 vs 模糊」 | 能 key 定位→文件；只能按意思找→向量 |
| retrieval 返回空串 | 库里没相似片段 | 先 `add` 足够的条目，或调大 `top_k` |

更多见 [新手入门与常见问题](新手入门与常见问题.md)。

---

## 7、本章小结与下一章

✅ 你现在已经能：

- 把「记忆」拆成三层：短期（会话内）/ 长期（文件）/ 向量（语义），并对应到内核
- 用「精确 vs 模糊」决胜：能 key 定位用 `FileMemory`，只能按意思找用 `VectorMemory`
- 说清「向量库是检索方式、不是记忆本身」，玩具嵌入与真 embedding 的区别
- 回答 RAG 与 Memory 的边界：读（现查）vs 写（持续沉淀）
- 用 `retrieved_context` 拼出 RAG 的最小闭环

➡️ 下一章 [**10 任务拆解与 Planning →**](10-任务拆解与Planning.md)（阶段四 State 层开局）：记忆让 Agent「记得住」，下一步要让 Agent「拆得动」——把一个大目标拆成计划（Plan-and-Execute），并和学过的手写循环（ReAct）做取舍。可回[教程目录大纲](教程目录大纲.md)看全局。