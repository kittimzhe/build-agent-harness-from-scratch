# 教程目录大纲

本大纲是《从零手写 Agent Harness：从 Prompt 到 Runtime》的导航索引，按知识脉络组织。每章下列出核心知识点。

> 这份大纲不是固定目录。Agent 技术栈在快速演进，新的 context / harness / runtime 知识点会持续并入这条主线。
>
> 📌 **当前进度**：已发布 `01`、`02`、`05`、`06`、`07`、`08`、`09`（「调用 → 状态 → 循环 → 容错 → 结构化 → 治理 → 记忆」主线七连，阶段三收官）；其余章节规划中，未发布的章节只列知识点，不做成可点链接，避免 404。

---

## 🟢 阶段一：Prompt 层 —— 让模型动起来

**阶段目标：跑通第一次 LLM 调用，理解消息状态与上下文窗口。**

### 01 LLM 调用与环境准备 ✅ 已发布
- Agent Harness 是什么、为什么不用现成框架 → [第 01 章](01-LLM调用与环境准备.md)
- OpenAI 兼容 SDK：API Key、模型名、Base URL 三件套 → [第 01 章](01-LLM调用与环境准备.md)
- 流式输出与同步调用的取舍；封装 `LLMClient`（Harness 第一块砖）

### 02 消息状态与上下文窗口 ✅ 已发布
- 消息角色 system/user/assistant、对话历史怎么攒；无状态 API 的本质 → [第 02 章](02-消息状态与上下文窗口.md)
- Token 计数与成本估算：多轮对话的滚雪球账单 → [第 02 章](02-消息状态与上下文窗口.md)
- 上下文窗口、KV Cache、长上下文为什么会失效；历史治理第一刀（截断） → [第 02 章](02-消息状态与上下文窗口.md)

### 03 Prompt 与结构化输出（规划中）
- Prompt Engineering 六要素、Few-shot
- JSON Mode、结构化输出约束
- 输出解析与重试自纠

---

## 🔵 阶段二：Tool 层 —— 给 Agent 装手脚

**阶段目标：从「能聊天」到「能用工具」，手写循环。**

### 04 Function Calling 原理（规划中）
- 从 Prompt 注入工具到原生 Function Calling
- Tool Schema 设计：可判别、token-efficient
- 工具返回结果怎么塞回上下文

### 05 手写第一个工具循环 ✅ 已发布
- Function Calling 最小闭环（schema / tool_calls / role="tool"）；Thought → Action → Observation 循环（ReAct 当教具讲透） → [第 05 章](05-手写第一个工具循环.md)
- 终止条件与最大轮数护栏（自然终止 vs 预算终止） → [第 05 章](05-手写第一个工具循环.md)

### 06 工具集合与容错 ✅ 已发布
- 多工具调度：让 LLM 自己选；工具失败在边界的三种兜底 → [第 06 章](06-工具集合与容错.md)
- 参数解析、错误捕获、重试、超时、幂等（ResilientTool / ToolRegistry） → [第 06 章](06-工具集合与容错.md)

### 07 结构化输出与 Schema 设计 ✅ 已发布
- Pydantic 约束输出、JSON Schema（extract_json / strict_validate / structured_chat） → [第 07 章](07-结构化输出与Schema设计.md)
- 自纠重试：解析失败时让模型自己修 → [第 07 章](07-结构化输出与Schema设计.md)

---

## 🟣 阶段三：Context 层 —— 管好模型的「工作内存」

**阶段目标：理解 Context Engineering，不再把上下文当静态 prompt。**

### 08 Context 治理：压缩、取舍、按需读取 ✅ 已发布
- 上下文污染与注意力退化（lost in the middle） → [第 08 章](08-Context治理.md)
- 什么时候压缩、什么时候只存引用按需读（compact / select_by_budget / ReferenceLibrary） → [第 08 章](08-Context治理.md)

### 09 记忆体系：文件 / 会话记忆 vs 向量记忆 ✅ 已发布
- 短期记忆 vs 长期记忆 vs 向量记忆（FileMemory / VectorMemory） → [第 09 章](09-记忆体系.md)
- 何时用文件记忆、何时用向量记忆（向量库作教具/附录） → [第 09 章](09-记忆体系.md)
- RAG 与 Memory 的边界 → [第 09 章](09-记忆体系.md)

---

## 🟠 阶段四：State 层 —— 让长任务可恢复

**阶段目标：让 Agent 能跑长任务，出错能恢复。**

### 10 任务拆解与 Planning（规划中）
- Plan-and-Execute 模式
- 与 ReAct 的取舍

### 11 失败策略与反思（规划中）
- Self-Reflection、失败重试（作为失败策略，不单独成主线范式）

### 12 Checkpoint 与状态恢复（规划中）
- 状态序列化、断点续跑
- Durable Execution 思路

---

## 🔴 阶段五：Runtime 层 —— 封装你的 Harness

**阶段目标：把前面能力封装成可复用的 mini Agent Runtime。**

### 13 封装 Mini Agent Runtime（规划中）
- Agent 状态机、事件循环
- 对比 LangGraph / OpenAI Agents SDK

### 14 可观测：Trace / 日志 / 回放（规划中）
- log / metric / trace / replay 四件套
- 接入 Langfuse

### 15 终止条件 · 权限 · 安全（规划中）
- Prompt Injection 与工具安全
- 最小授权、审批、sandbox

---

## 🟤 阶段六：Protocol 与实战

**阶段目标：接入协议层，交付一个完整项目。**

### 16 MCP / A2A / Skills 接入（规划中）
- MCP 原理、自建 MCP Server
- A2A 与 Skills 资产化

### 17 实战项目：深度研究助手 Agent（规划中）
- 联网搜索 + RAG + 多步规划 + 报告生成（强制用上 Checkpoint / Trace / 终止条件）

### 18 部署交付：FastAPI + Docker（规划中）
- 从 demo 到可上线服务

---

## 📎 附录

- [全书术语表](全书术语表.md)
- [面试题库](面试题库.md)
- [新手入门与常见问题](新手入门与常见问题.md)
- [工具导航与参考资料索引](工具导航与参考资料索引.md)
- [教程更新日志](教程更新日志.md)

---

> 🧭 **关于大纲重心的一点说明**：本教程主线是「从 Prompt 到 Runtime」，不是「又一套手写 Agent 范式课」。因此 ReAct / 手写向量库 / 反思这些**作为教具讲清即可，不占主线篇幅**；主线篇幅给工具失败、超时、幂等、checkpoint、trace、权限——这些才是 2026 拉开差距的东西。具体编排已在上面体现（ReAct 收为一章讲透，向量库降为「记忆体系」里的一节，反思并入「失败策略」）。
