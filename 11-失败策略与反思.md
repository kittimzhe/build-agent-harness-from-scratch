# 11 - 失败策略与反思

> 📌 **第 11 章 · 阶段四 State 层** · [← 返回目录大纲](教程目录大纲.md) · [上一章 10 任务拆解与 Planning →](10-任务拆解与Planning.md) · [下一章 12 Checkpoint 与状态恢复 →](12-Checkpoint与状态恢复.md)

---

**本章课程目标：**

- 分清两种「重试」：第 06 章的**傻重试**（同动作退避重来，挡瞬时故障）和本章的**反思式重试**（失败 → 反思 → 换思路，挡「思路错了」）。
- 掌握 **Self-Reflection** 的本质：把失败信息回喂给模型，让它思考「为什么失败、该换什么」——和第 07 章「把解析错误回喂自纠」是同一招，只是粒度从**格式**升到**任务**。
- 落地第九块砖：`harness/reflection.py`（`reflect` / `retry_with_reflection` / `Reflection`）。
- 交代清楚「失败策略是配角、不单独成主线范式」——它服务于 05（循环）和 10（计划），而不是第三种 Agent 范式。

**学习建议：** demo ①–④ 用 `FakeReflector` 确定性输出、**无需 API Key**；⑤ 才走真实 LLM。本章和第 06 章是一对，放一起看才能分清「什么时候傻重试、什么时候请模型反思」。

---

## 1、失败策略不单独成范式，它服务谁

第 05 章（ReAct）和第 10 章（Plan-and-Execute）是两种**干活范式**。但「失败了怎么办」不是第三种范式——它是**给前两者兜底的策略**：

- 给 **ReAct** 兜底：某一轮 tool 调用失败，是直接停机，还是先反思一下再出下一招？
- 给 **P&E** 兜底：计划某一步 `failed` 了（第 10 章我们「失败即停」），停完之后呢？

所以大纲里写得很克制：**Self-Reflection、失败重试，作为失败策略，不单独成主线范式**。它不改变「怎么干活」，只改变「干砸了怎么站起来」。

| | 傻重试（06） | 反思式重试（11） |
| --- | --- | --- |
| 触发 | 瞬时故障（超时/限流/抖动） | 「动作/思路本身可能错了」 |
| 手段 | 同一动作 + 退避 | 回喂错误 → 模型想改法 → 换动作 |
| 有没有模型参与 | 无 | 有 |
| 内核 | `RetryPolicy` / `run_with_policy` | `reflect` / `retry_with_reflection` |
| 典型对象 | 一次 HTTP 调用 | 一个任务/一个计划步骤 |

---

## 2、Self-Reflection 的本质：把失败「喂」回去

第 07 章的自纠重试是：模型输出 JSON 解析失败 → 把 `解析错误` 附加回消息 → 再输出。本章是同一个动作，只是喂回去的不是格式错误，而是**任务失败信息**：

```python
reflect(llm, goal="查订单物流", failure_log="内网直连超时")
# → Reflection(verdict="retry", reason="内网被防火墙挡", next_action="改走官网开放接口")
```

模型拿到失败信息，输出三件套：**verdict**（继续 `retry` 还是放弃 `give_up`）、**reason**（为什么失败）、**next_action**（下一步具体怎么改）。这三点正好喂给执行层去「换思路再试」。

> 关键在 `next_action`：如果反思只产出「再试一次」，那就退化成了傻重试。**真反思要产出「换个什么」**——这才是 Self-Reflection 和 `RetryPolicy(max_retries=3)` 的分水岭。

---

## 3、内核：harness/reflection.py（第九块砖）

### 3.1 reflect —— 一次反思

```python
r = reflect(llm, goal, failure_log)
# 内部复用第 07 章 structured_chat，输出 {verdict, reason, next_action}
```

### 3.2 retry_with_reflection —— 反思式重试循环

```python
res = retry_with_reflection(llm, goal, attempt_fn, max_reflections=2)
# attempt_fn(当前目标) -> str：成功返回结果，失败抛异常
# 失败 → reflect → verdict=retry 就用 next_action 换目标再试；give_up 或超限即停
```

看它和 `run_with_policy`（06）的边界：**06 在同一动作上重试 N 次、从不换；11 每次重试都可能是不同动作。** 两边都不该乱用——瞬时故障交给 06（便宜、快），「思路错了」才请 11（贵、但真的在想）。完整实现见 [harness/reflection.py](harness/reflection.py)。

> 🧱 第九块砖落位：到这里，Agent 干砸了有策略兜底——傻重试扛瞬时故障、反思式重试扛思路错误。但还有一个缺口：**干到一半崩了，前面的成果能找回来吗？** 这就是第 12 章 Checkpoint 要补的。

---

## 4、串联：失败策略与前面每一章

- **05**：`AgentLoop` 某轮 tool 失败时，可将报错作为反思的输入，产出下一轮的 action。
- **06**：`RetryPolicy`（傻重试）挡瞬时故障，`reflect`（聪明重试）挡思路错，两者边界在 §1 的表。
- **07**：`reflect` 直接复用 `structured_chat`——格式自纠与任务反思，同一个回喂套路。
- **10**：`execute_plan` 失败即停，`reflect` 决定「这一步换思路重做」还是「整个计划重规划」。
- **12**：`ReflectionResult.attempts`（每一轮都记下来）是可序列化的状态，正是第 12 章断点续跑的原料。

---

## 5、运行本章案例

demo ①–④ 无需 `.env`，⑤ 需要（配置见 [第 01 章环境准备](01-LLM调用与环境准备.md#3、环境准备)）：

```bash
python examples/11_reflection.py
```

预期输出（①–④ 是确定性的）：

```
① 第 06 章『傻重试』vs 本章『反思式重试』
  傻重试（RetryPolicy）：同一个动作、退避重来
  聪明重试（retry_with_reflection）：失败 → 反思 → 换思路 → 再试
  RetryPolicy 的语义：max_retries=3（同一动作再试 3 次），base_delay=0.01s 退避

② reflect：把失败回喂给模型，拿回结构化反思
  verdict     = retry
  reason      = 当前思路被证明走不通
  next_action = 改成从官网开放接口去查，而不是内网直连

③ retry_with_reflection：失败 → 反思 → 换思路 → 再试
  ❌ 第1次 goal=查订单 42 的物流… → 内网直连被拒：…，反思 verdict=retry
  ✅ 第2次 goal=改成从官网开放接口去查… → 查到了：物流单在派送中
  最终 success=True，结果：查到了：物流单在派送中

④ 反思融入 Planning：计划步骤失败，模型决定下一步
  计划第 0 步『拉订单数据』执行失败 → 反思给改法：
    retry：改成从官网开放接口去查，而不是内网直连

⑤ 真实反思：让真模型反思一次失败并给改法
  verdict     = …（真实模型生成）
  reason      = …
  next_action = …
```

> ⚠️ demo⑤ 由真实模型生成，措辞不同——**看质量**：它有没有给出「具体哪里错 + 具体怎么改」，而不是一句「再试一次」的废话。

---

## 6、常见报错排查

| 报错 / 现象 | 原因 | 解决 |
| --- | --- | --- |
| 反思永远「再试一次」，没实际改法 | 反思指令没逼它给 `next_action` | 在 reflect 的 system 里强调「必须写清具体改法」 |
| 反思输出不是 `retry/give_up` | 没走结构化输出，模型自由发挥 | 用 `structured_chat` + `Literal["retry","give_up"]` 锁死 |
| 瞬时故障却被请模型反思 | 把「傻重试」的活派给了「聪明重试」 | 超时/限流走 `RetryPolicy`，思路错才走 reflect |
| `retry_with_reflection` 退出但没成功 | 反思次数用尽或 verdict=give_up | 看 `res.attempts` 每一步记录定位 |
| 反思循环死循环 | next_action 每次都换汤不换药 | 给 `max_reflections` 设上限（默认 2） |

更多见 [新手入门与常见问题](新手入门与常见问题.md)。

---

## 7、本章小结与下一章

✅ 你现在已经能：

- 分清傻重试（06，同动作退避）与反思式重试（11，回喂错误换思路）的边界
- 用 `reflect` 把任务失败回喂给模型，拿回 `verdict/reason/next_action` 三件套
- 用 `retry_with_reflection` 跑「失败 → 反思 → 换思路 → 再试」的循环
- 说清「失败策略是给 05/10 兜底的配角，不是第三种范式」
- 理解思反需要产出「具体改法」而非「再试一次」

➡️ 下一章 [**12 Checkpoint 与状态恢复 →**](12-Checkpoint与状态恢复.md)（阶段四收官）：现在 Agent 干砸了能反思、能重试，但**进程一崩、前面的成果全丢**——下一章把计划状态、反思轨迹序列化落盘，做到断点续跑（Durable Execution 思路）。可回[教程目录大纲](教程目录大纲.md)看全局。