# 完整依赖 = 核心依赖 + 部署（FastAPI/uvicorn）与文档加载等可选能力
# 离线案例用 requirements.txt 就够；跑第 18 章服务冒烟才需要本文件

-r requirements.txt

# 阶段三起：向量与数值计算（手写最简向量库）
numpy>=1.26.0

# 阶段三起：文档加载与切分
pypdf>=4.0.0
markdown>=3.5

# 阶段六：部署
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# 可观测（第 14 章可选）
# langfuse>=2.0.0
