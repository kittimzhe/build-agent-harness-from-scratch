# 完整依赖 = 核心依赖 + 部署（FastAPI/uvicorn）与文档加载等可选能力
# 离线案例用 requirements.txt 就够；跑第 18 章服务冒烟才需要本文件

-r requirements.txt

# 阶段六：部署（第 18 章服务）
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# 依赖即承诺：requirements 只放真实 import 的东西。
# 曾经列过 numpy/pypdf/markdown——教程正文自写了最简向量库（harness/memory.py），
# 文档加载也没做，列着就是骗人。要加回来，先让代码真的 import 它。

# 可观测（第 14 章可选）
# langfuse>=2.0.0
