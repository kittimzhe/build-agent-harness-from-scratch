# 新手入门与常见问题

本篇解决「第一次跑本仓库案例」的各类环境与报错问题。遇到问题先看这里。

---

## 1、环境准备

- **推荐 Python 版本**：3.10（支持 3.10–3.13，不建议 3.14）
- **创建虚拟环境**：

  ```bash
  python3.10 -m venv .venv
  source .venv/bin/activate        # macOS/Linux
  # .venv\Scripts\activate         # Windows
  pip install -r requirements.txt
  ```

## 2、API Key 怎么申请

| 提供商 | 申请地址 | 说明 |
| --- | --- | --- |
| DeepSeek | https://platform.deepseek.com/ | 推荐，便宜稳定，兼容 OpenAI 协议 |
| 通义千问/百炼 | https://bailian.console.aliyun.com/ | 国内大厂，模型多 |
| OpenAI | https://platform.openai.com/ | 需能访问 |

申请后在根目录 `.env` 填入，变量名与 `.env-example` 一致。

## 3、用 Ollama 免 Key 跑通

不想用云 API？用本地 Ollama：

1. 安装 Ollama：https://ollama.com/
2. 拉模型：`ollama pull qwen2.5:7b`
3. 在 `.env` 设 `LLM_PROVIDER=ollama`，按需改 `OLLAMA_MODEL`

## 4、运行案例的约定

- **必须在项目根目录运行** `python`，否则读不到 `.env`：

  ```bash
  python 案例与源码-1-Prompt层/01-HelloLLM.py
  ```

## 5、常见报错与解决

### `ModuleNotFoundError: No module named 'openai'`
→ 虚拟环境没激活或没装依赖，重跑 `pip install -r requirements.txt`。

### `AuthenticationError` / 401 / 403
→ `.env` 里 API Key 没填、填错、或没读到。确认在**根目录**运行，且 `LLM_PROVIDER` 设置正确。

### 找不到 `.env`
→ 确认把 `.env-example` 复制为 `.env`，不是改名。

### 连接超时
→ 国内访问 OpenAI 需代理；建议改用 DeepSeek / 通义千问。

---

> 还有问题？欢迎提 [Issue](https://github.com/kittimzhe/build-agent-harness-from-scratch/issues)。
