# 新手入门与常见问题

本篇解决「第一次跑本仓库案例」的各类环境与报错问题。遇到问题先看这里。

---

## 1、环境准备

- **推荐 Python 版本**：3.10（支持 3.10–3.13）。不建议 3.14——部分二进制依赖（如 numpy 预编译轮子）在 3.14 刚发布时往往还没跟上，装包容易失败，等生态稳定再升。
- **创建虚拟环境**：

  ```bash
  python3.10 -m venv .venv
  source .venv/bin/activate        # macOS/Linux
  # .venv\Scripts\activate         # Windows
  pip install -r requirements.txt
  ```

## 2、API Key 怎么申请

| 提供商 | 申请地址 | 说明 |
| --- | --- | --- |
| DeepSeek | https://platform.deepseek.com/ | 推荐，便宜稳定，兼容 OpenAI 协议 |
| 通义千问/百炼 | https://bailian.console.aliyun.com/ | 国内大厂，模型多 |
| OpenAI | https://platform.openai.com/ | 需能访问 |

申请后在根目录 `.env` 填入，变量名与 `.env-example` 一致。

## 3、用 Ollama 免 Key 跑通

不想用云 API？用本地 Ollama：

1. 安装 Ollama：https://ollama.com/
2. 拉模型：`ollama pull qwen2.5:7b`
3. 在 `.env` 设 `LLM_PROVIDER=ollama`，按需改 `OLLAMA_MODEL`

## 4、运行案例的约定

- 代码用 `find_dotenv(usecwd=True)` 自动向上查找 `.env`，所以**在仓库任意子目录运行都能读到配置**，不必非在根目录：

  ```bash
  python examples/01_hello_llm.py      # 第 01 章：同步 + 流式
  python examples/02_chat_history.py   # 第 02 章：记忆 / token 账单 / 截断
  python examples/05_tool_loop.py      # 第 05 章：工具循环 / 护栏
  python examples/06_tool_retry.py     # 第 06 章：重试 / 超时 / 幂等（①–③ 无需 API）
  python examples/07_structured_output.py  # 第 07 章：结构化输出 / 自纠（①–③ 无需 API）
  python examples/08_context_governance.py # 第 08 章：压缩 / 取舍 / 按需读取（①–④ 无需 API）
  python examples/09_memory.py             # 第 09 章：文件 / 向量记忆（全部无需 API）
  python examples/10_planning.py           # 第 10 章：任务拆解 / Planning（①–④ 无需 API）
  python examples/11_reflection.py          # 第 11 章：失败策略 / 反思（①–④ 无需 API）
  python examples/12_checkpoint.py          # 第 12 章：断点续跑 / 状态恢复（全部无需 API）
  python examples/13_runtime.py             # 第 13 章：封装 Mini Agent Runtime（①–③ 无需 API）
  python examples/14_trace.py               # 第 14 章：可观测 Trace/日志/回放（①–④ 无需 API）
  python examples/15_safety.py              # 第 15 章：终止条件·权限·安全（全部无需 API）
  python examples/16_protocol.py             # 第 16 章：MCP/A2A/Skills 接入（全部无需 API）
  ```

  > 唯一要求：`.env` 在仓库根目录（和 `.env-example` 同级）。
  >
  > 05 的 demo①/③ 依赖模型「愿意调工具」。如果输出里 `终止方式：model` 而你预期是 `max_rounds`，多半是模型直接用文本回答了——重跑一次，或把问题改得更强制（比如「必须调用 get_current_time 工具」）。

## 5、常见报错与解决

### `ModuleNotFoundError: No module named 'openai'`
→ 虚拟环境没激活或没装依赖，重跑 `pip install -r requirements.txt`。

### `AuthenticationError` / 401 / 403
→ `.env` 里 API Key 没填、填错、或 `LLM_PROVIDER` 设错。先确认 `.env` 在仓库根目录、Key 与 provider 匹配（如 `LLM_PROVIDER=deepseek` 对应 `DEEPSEEK_API_KEY`）。

### 找不到 `.env`
→ 代码用 `find_dotenv()` 向上找，只要 `.env` 在仓库根目录（和 `.env-example` 同级）即可。确认是把 `.env-example` **复制**为 `.env`，不是改名。

### 连接超时
→ 国内访问 OpenAI 需代理；建议改用 DeepSeek / 通义千问。

---

> 还有问题？欢迎提 [Issue](https://github.com/kittimzhe/build-agent-harness-from-scratch/issues)。
