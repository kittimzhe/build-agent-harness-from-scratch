# 05 - 手写第一个工具循环

> 📌 **第 5 章 · 阶段二 Tool 层** · [← 返回目录大纲](教程目录大纲.md) · [上一章 02 消息状态与上下文窗口 →](02-消息状态与上下文窗口.md) · 下一章 06 工具集合与容错（规划中）

---

**本章课程目标：**

- 写出 Agent 的心脏：**工具循环**——模型想（Thought）→ 调工具（Action）→ 看结果（Observation）→ 直到给出最终回答。这套循环有个学名 ReAct，但记住：**循环才是本体，ReAct 只是教具**。
- 掌握 Function Calling 的最小闭环：工具怎么声明（schema）、模型怎么表达「我要调工具」（`tool_calls`）、结果怎么回喂（`role="tool"`）。
- 给循环装上第一道护栏：`max_rounds` 最大轮数——没有它，一个反复要工具的模型能把你的账单烧穿。
- 顺手升级内核：`LLMResult.tool_calls` 从 SDK 对象收成 `list[dict]`，上层从此完全不碰 SDK。

**学习建议：** 这章是全教程的第一个分水岭。第 01 章你会调模型，第 02 章你会管状态，这一章之后你才真正有了一个「会做事的 Agent」。写完务必跑案例，盯着打印出的执行轨迹看：`tool_calls` 长什么样、`role="tool"` 的消息怎么喂回去、护栏什么时候触发。

**官方文档与资源**：[OpenAI Function Calling](https://platform.openai.com/docs/guides/function-calling) · [DeepSeek Function Calling](https://api-docs.deepseek.com/guides/function_calling) · 更多见 [工具导航](工具导航与参考资料索引.md)。

---

## 1、从聊天到行动：缺的那一环

第 02 章结束时，`ChatSession` 已经能记住上文了——但它只会**说**，不会**做**。你问「现在几点」，它只能猜。缺的一环：让模型能调用你写的函数。

### 1.1 Function Calling 最小速成

给模型装工具，只要三件事（本节先建立直觉，代码在 §3）：

**① 声明工具（schema）**——告诉模型「有哪些工具、各吃什么参数」：

```python
{
    "type": "function",
    "function": {
        "name": "add",
        "description": "两数相加",
        "parameters": {
            "type": "object",
            "properties": {"a": {"type": "number"}, "b": {"type": "number"}},
            "required": ["a", "b"],
        },
    },
}
```

**② 模型表达「我要调工具」**——返回里不再只有文本，还可能带 `tool_calls`：

```python
[{"id": "call_abc123", "type": "function",
  "function": {"name": "add", "arguments": "{\"a\": 1234, \"b\": 5678}"}}]
```

注意 `arguments` 是**字符串化的 JSON**——模型生成的是文本，解析是你的事（坑见第 6 节）。

**③ 你执行后回喂结果（Observation）**——第四种消息角色 `tool` 登场：

```python
{"role": "tool", "tool_call_id": "call_abc123", "content": "6912"}
```

`tool_call_id` 必须对得上 ② 里的 `id`，模型才知道这条结果回答的是哪次请求。

> 第 02 章说消息列表是「对话记录」；从这章起它变成**执行日志**：user → assistant(tool_calls) → tool → assistant(tool_calls) → tool → assistant(最终回答)。回头看，这就是 Agent 的全部骨架。

### 1.2 为什么不是「让模型输出 JSON 我解析」

你可能会想：不用 Function Calling，prompt 里写「请输出 {"tool": "add", ...}」不也行？行，但要自己处理：模型输出多余废话、JSON 格式错误、调不存在的工具……Function Calling 是提供商在**解码层**做的约束，稳定性高一个量级。这条「prompt 约束 → 原生协议」的演进线，第 04 章展开。

---

## 2、内核升级：tool_calls 收成 list[dict]

第 01 章立过规矩：**不漏 SDK 对象给上层**。之前 `tool_calls` 直接透传了 SDK 对象——欠的债这章还上。`harness/llm.py` 的 `_to_result()` 里加一步转换：

```python
tool_calls = [
    {"id": tc.id, "type": "function",
     "function": {"name": tc.function.name, "arguments": tc.function.arguments}}
    for tc in (getattr(msg, "tool_calls", None) or [])
]
```

收益立刻兑现：`AgentLoop` 把 `result.tool_calls` **原样 append 回 messages** 就能发起下一轮请求，全程不用 import OpenAI SDK。`chat()` 签名一个字没动——这就是「内核只加能力」。

---

## 3、手写 AgentLoop：第三块砖

> 这 100 行代码，对应框架里的什么？

| 本教程 | 框架里对应的东西 |
| --- | --- |
| `AgentLoop` | OpenAI Agents SDK 的 `Runner`、LangGraph 的图循环 |
| `Tool(func, parameters=...)` | Agents SDK 的 `@function_tool`、LangChain 的 `@tool` |
| `run()` 返回 `{reply, rounds, messages, stopped_by}` | 框架的 `RunResult`（含轮次 / 终止原因） |
| `max_rounds` 护栏 | 框架的 `max_turns` / recursion_limit |

### 3.1 设计要点

1. **循环只判一件事**：这轮返回里有没有 `tool_calls`。没有 → 模型认为任务完成，循环结束（**自然终止**）。
2. **工具失败不炸循环**：参数不是 JSON、工具名不存在、执行抛异常——全部捕获，把错误文本作为 Observation 回喂，让模型自己纠。这是第 06 章「容错」的第一课。
3. **护栏兜底**：`max_rounds` 到了还没结束，硬停（**预算终止**）。返回里用 `stopped_by` 区分两种死法。

### 3.2 实现

完整代码见 [harness/loop.py](harness/loop.py)。循环主体只有二十行：

```python
for round_no in range(1, self.max_rounds + 1):
    result = self.llm.chat(messages, tools=[t.schema() for t in self.tools.values()])

    if not result.tool_calls:            # 终止条件①：自然终止
        messages.append({"role": "assistant", "content": result.content})
        return {"reply": result.content, "rounds": round_no,
                "messages": messages, "stopped_by": "model"}

    messages.append({"role": "assistant",     # 记下模型要调什么
                     "content": result.content or None,
                     "tool_calls": result.tool_calls})
    for tc in result.tool_calls:             # 逐个执行、回喂
        messages.append(self._execute(tc))

return {"reply": "（被护栏终止）", ..., "stopped_by": "max_rounds"}   # 终止条件②
```

`_execute()` 是工具执行器：解析参数 → 找工具 → 执行 → 包装成 `role="tool"` 消息；任何一步失败都变成错误文本回喂（完整实现见源码）。

### 3.3 用起来

```python
from harness import AgentLoop, Tool

def add(a: float, b: float) -> float:
    """两数相加"""
    return a + b

tools = [Tool(add, parameters={...})]
loop = AgentLoop(tools=tools, max_rounds=8)
out = loop.run("用工具算 1234 加 5678")
print(out["reply"])       # 6912
print(out["stopped_by"])  # "model" —— 模型自然终止
```

> 🧱 第三块砖落位：`LLMClient`（调用）+ `ChatSession`（状态）+ `AgentLoop`（循环）——**调用 → 状态 → 循环，三部曲收官，你已经有一个真正的 Agent 了。** 后面的章节都是往这副骨架上加器官。

---

## 4、终止条件与护栏：循环的「方向盘」

会转的循环不稀奇，**会停**的循环才可靠。两类终止：

| 终止方式 | 触发 | 本教程 | 风险 |
| --- | --- | --- | --- |
| 自然终止 | 模型不再要工具，给出最终回答 | `stopped_by="model"` | 模型可能提前认输（任务没做完就答） |
| 预算终止 | 轮数 / token / 超时撞上限 | `stopped_by="max_rounds"` | 任务被腰斩，要处理半成品状态 |

`max_rounds` 看起来土，但它是所有 Agent 框架的标配（Agents SDK 叫 `max_turns`，LangGraph 叫 `recursion_limit`）。原因很现实：**模型不存在「一定会停」的保证**——工具报错它重试、结果不满意它再查，很容易转起来不停。

> 进阶预告：真实系统还有第三类——**外部干预终止**（人工审批、权限拒绝、用户中断），以及半成品状态的 checkpoint 恢复。分别是第 15 章和第 12 章的事。

顺带把 ReAct 放回它该在的位置：1953 年的心理学概念、2022 年的提示词模式（在 prompt 里要求输出 Thought:/Action:/Observation:）。今天 Function Calling 原生化了这套结构，**你写的是循环，ReAct 是理解它的教具**——面试时能讲清这个演化，比背 ReAct 定义加分。

---

## 5、运行本章案例

确保 `.env` 配置可用（[第 01 章环境准备](01-LLM调用与环境准备.md#3、环境准备)），然后在仓库任意目录运行：

```bash
python examples/05_tool_loop.py
```

预期输出（三个 demo，② 是重头戏）：

```
✅ 环境自检通过：provider=deepseek

========================================================
① 单轮工具调用：模型自己决定用 add
========================================================
最终回答：1234 加 5678 等于 6912。
轮数：2，终止方式：model
  执行轨迹（消息列表）：
    👤 user        → 用工具算一下 1234 加 5678 等于多少
    🤖 assistant  → 请求工具 add({"a": 1234, "b": 5678})
    🔧 tool       → 6912
    🤖 assistant  → 1234 加 5678 等于 6912。

========================================================
② 多轮链式：先查时间 → 再用结果算账
========================================================
最终回答：现在是 21:07，今天已经过了 1267 分钟。
轮数：3，终止方式：model
  执行轨迹（消息列表）：
    👤 user        → 先查一下现在几点，然后用 add 工具算出今天过了多少分钟…
    🤖 assistant  → 请求工具 get_current_time({})
    🔧 tool       → 2026-08-24 21:07:33
    🤖 assistant  → 请求工具 add({"a": 1260, "b": 7})
    🔧 tool       → 1267
    🤖 assistant  → 现在是 21:07，今天已经过了 1267 分钟。
💡 第 2 轮的 add 参数来自第 1 轮工具结果——这就是 Observation 喂回下一轮 Thought

========================================================
③ 护栏：max_rounds=1 时，链式任务被硬停
========================================================
最终回答：（已达到最大轮数 1，循环被护栏终止）
轮数：1，终止方式：max_rounds
💡 没有护栏，一个反复要工具的模型会把你的账单烧穿
```

> 💡 盯着 ② 的轨迹看：第 2 轮 `add` 的参数（1260、7）来自第 1 轮的工具结果——**上一轮的 Observation 变成下一轮的 Thought 的输入**，这就是循环会「链式干活」的原因。

---

## 6、常见报错排查

| 报错 / 现象 | 原因 | 解决 |
| --- | --- | --- |
| `Invalid parameter: messages ... tool_call_id` | `role="tool"` 消息没带 `tool_call_id`，或对不上 assistant 里的 `id` | 用 `_execute()` 的封装，别手工拼 tool 消息 |
| `json.JSONDecodeError` | 模型给的 `arguments` 不是合法 JSON（偶尔发生） | 已在 `_execute()` 捕获并回喂；第 06 章加重试 |
| `arguments` 是 `"{}"` 但工具需要参数 | schema 没写 `required`，模型以为可省 | schema 里标清 `required`（第 04 章展开） |
| 回喂后模型重复要同一个工具 | 工具返回了错误文本，模型在重试 | 看工具输出内容；确实失败要让模型知道原因 |
| 400：`tools` 参数不支持 | provider / 模型不支持 Function Calling | DeepSeek `deepseek-chat` 支持；换 provider 前查文档 |

更多见 [新手入门与常见问题](新手入门与常见问题.md)。

---

## 7、本章小结与下一章

✅ 你现在已经能：

- 讲清 Function Calling 最小闭环：schema 声明 → `tool_calls` 表达 → `role="tool"` 回喂
- 手写工具循环 `AgentLoop`：判断终止、执行工具、结果回喂、异常兜底
- 用 `max_rounds` 护栏防止死循环，区分自然终止与预算终止
- 说清 ReAct 的定位：**循环是本体，ReAct 是教具**

➡️ 三部曲收官：**调用（01）→ 状态（02）→ 循环（05）**，你手里的 `harness/` 已经是一个能干活的 Agent 内核。下一章 **06 工具集合与容错**（规划中）：重试、超时、幂等——真实世界的工具会失败，Agent 要扛得住。可回[教程目录大纲](教程目录大纲.md)看全局。
