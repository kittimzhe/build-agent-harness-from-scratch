# 教程更新日志

本文件记录《从零手写 Agent Harness：从 Prompt 到 Runtime》的内容变更。

格式：`## YYYY-MM-DD` + 变更摘要。

---

## 2026-08-24（三）

### 🎉 发布第 05 章《手写第一个工具循环》——「调用 → 状态 → 循环」三部曲收官
- 正文：Function Calling 最小闭环（schema / tool_calls / role="tool"）、手写 AgentLoop、终止条件与护栏（自然终止 vs 预算终止）、「循环是本体，ReAct 是教具」的定位。
- 内核第三块砖：`harness/loop.py` 的 `Tool` + `AgentLoop`（工具包装 / 循环 / max_rounds 护栏 / 工具异常回喂），`run()` 签名从本章冻住。
- 还代码债（上轮建议 §2.4）：`LLMResult.tool_calls` 从 SDK 对象收成 `list[dict]`，上层（AgentLoop）全程不 import SDK。
- 配套案例 `examples/05_tool_loop.py`：单轮工具调用、多轮链式（先查时间再算账）、护栏硬停三个 demo，并打印执行轨迹（消息列表角色流）。
- 进度标识同步：侧边栏 05 转链接、README 徽章「已发布 01·02·05」、大纲 05 节加链接、第 02 章文末链向 05。

---

## 2026-08-24（二）

### 🎉 发布第 02 章《消息状态与上下文窗口》
- 正文：无状态 API 本质（消息列表 = Agent 第一个状态）、Token 滚雪球账单、上下文窗口与 KV Cache（前缀稳定守则）、长上下文三个坑、历史治理第一刀（截断）。
- 内核第二块砖：`harness/session.py` 的 `ChatSession`（攒历史 / 累积用量 / 截断），`ask()` 签名从本章冻住。
- 配套案例 `examples/02_chat_history.py`：多轮记忆、每轮 token 增量、截断前后对比三个 demo；案例的环境自检直接读内核 `PROVIDERS`，不再重复维护提供商表。
- 进度标识同步：侧边栏 02 转为可点链接，README 徽章改「已发布 01–02」，大纲 02 节加链接。

### 🔧 章名全局对齐
- 侧边栏「05 ReAct 循环 → 05 工具循环」「11 反思与自纠 → 11 失败策略与反思」「09 记忆体系：文件 vs 向量」，与 README / 大纲一致。
- 第 01 章导语、阶梯图、5.1/5.2 提示、5.3 意义段清除 ReAct/RAG 旧表述，统一为「工具循环 / 消息状态 / 记忆」新主线。

---

## 2026-08-24（一）

### ✅ 已发布第 01 章
- 第 01 章《LLM 调用与环境准备》：调用三件套、同步/流式、封装 `LLMClient`。
- 配套案例 `examples/01_hello_llm.py`。

### 🔧 完成度诚实化（按修改建议 P0–P3）
- 侧边栏：未发布章节标「规划中」，不再做成可点链接（避免 404）。
- README 加真实进度徽章与「已发布 01」说明。
- 代码重构为会生长的内核：`harness/`（内核）+ `examples/`（案例），案例用「相对仓库根的 `sys.path`」import 内核（内核本身不靠它），配置改用 `find_dotenv`；`LLMClient` 对外返回 `LLMResult`，接口从第 01 章起冻住。
- 第 01 章页眉加章节导航；代码链接标「可本地运行」。
- `.env-example` 补齐 OpenAI 两项；依赖拆为 `requirements.txt`（轻）+ `requirements-full.txt`（重）。
- docsify `index.html` 补 repo 地址；GitHub Pages 发布范围缩小到站点文件。
- LICENSE 版权行改作者名。

### 🔧 文档与代码对齐（2026-08-24）
- 清掉「旧世界」残留：FAQ 第 4 节、第 01 章 3.4 / 第 7 节不再写「必须在根目录跑」，改为 `find_dotenv` 任意子目录可读 `.env`；第 01 章文末「下一章」死链改为纯文本「规划中」。
- `教程目录大纲.md` 02–18 章去掉可点链接，只列知识点，避免 404。
- CONTRIBUTING 的 `sys.path` 策略与案例代码对齐（方案 A：案例允许相对仓库根的 `sys.path`，内核不靠它）。
- README 首页卖点对齐「从 Prompt 到 Runtime」主线：上下文治理 / 工具失败 / checkpoint / trace / 权限为主，向量库 / 反思降为教具。

---

## 2026-08-23

### 🎉 仓库初始化
- 确立教程主线：**从 Prompt 到 Runtime**，聚焦「从零手写 Agent Harness」。
- 完成仓库骨架：README、教程目录大纲、6 阶段 18 章规划、附录占位。
- 配套 docsify 在线阅读站（`index.html` + `_sidebar.md`）。
- 配置文件就绪：`.gitignore`、`.env-example`、`requirements.txt`、`LICENSE`。
