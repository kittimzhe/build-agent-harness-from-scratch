# 教程更新日志

本文件记录《从零手写 Agent Harness：从 Prompt 到 Runtime》的内容变更。

格式：`## YYYY-MM-DD` + 变更摘要。

---

## 2026-08-26（十五）

### 🎉 发布第 18 章《部署交付：FastAPI + Docker》——全书收官
- 正文：demo 到上线的三层（服务化/容器化/可运行）；三条部署原则（无状态/只露协议/密钥不进镜像）；FastAPI 两端点；Dockerfile 三层；内核复用收官一页；运行案例 + 常见报错 + 全书收官回顾。
- 新增 `deploy/`：`app.py`（GET /health + POST /research，`DEEP_RESEARCH_OFFLINE=1` 离线冒烟）、`Dockerfile`（依赖层/代码层/启动命令，不含 .env）、`README.md`。
- 附录补齐：术语表补服务化/容器化/无状态/只露协议/密钥不进镜像；面试题库新增第 18 章 5 题带答法要点。
- 进度标识同步：侧边栏 18 转链接+阶段六全绿、README 徽章改为「全部章节完成」、大纲 18 节加链接、第 17 章文末链向 18、FAQ/README 补 18 命令。
- 至此全书 01/02/05…18 全部 16 个主线章节 + 4 个附录文件发布完毕，主线「调用 → … → 上服务」闭环。

---

## 2026-08-26（十四）

### 🎉 发布第 17 章《实战项目：深度研究助手 Agent》——把 01–16 全部串起来
- 正文：深度研究 = 规划 → 检索 → 综合三层数据流；为什么强制用上 Checkpoint/Trace/终止条件/记忆；项目结构；每块砖复用清单；运行案例 + 常见报错。
- 新增项目 `projects/deep_research/`：
  - `search.py`：`FakeSearchEngine`（确定性离线语料 + 中文 2-gram 轻量分词）+ `make_search_tool`（签名统一，真接 Tavily/Bing 只改 engine）。
  - `agent.py`：`DeepResearchAgent`——复用 make_plan(10) / search+记笔记+向量记忆(09) / 反思换词(11) / run_plan_with_checkpoint 断点续跑(12) / tracer(14) / StopConditions(15)，落盘 checkpoint + notes + trace。
  - `main.py`：`--offline`（ScriptedLLM + FakeSearchEngine，无需 API）/ `--question`（LLMClient 真实研究）。
  - `README.md`：数据流、运行、工件、怎么换真。
- 附录补齐：术语表补 DeepResearchAgent/FakeSearchEngine/深度研究/离线替身换真；面试题库新增第 17 章 5 题带答法要点。
- 进度标识同步：侧边栏 17 转链接、README 徽章「01…17」、大纲 17 节加链接、第 16 章文末链向 17、FAQ/README 补 17 命令；.gitignore 加 `.deep_research*/` 运行时产物。
- 验证：离线全流程 + 断点续跑（新进程零重跑）+ 四大工件（checkpoint/notes/trace）落盘、final_state=done。

---

## 2026-08-26（十三）

### 🎉 发布第 16 章《MCP / A2A / Skills 接入》——阶段六 Protocol 与实战开局
- 正文：三协议三句话（MCP 接工具生态、A2A 接别的 Agent、Skills 接知识资产）、共同接入缝「适配成 Tool」、MCP 谁定义/谁消费解耦、A2A 专家也是工具、Skills 按需挂载、串联 05/06/09/13/14/15/17。
- 内核第十四块砖：`harness/protocol.py` 的 `MCPTool`/`MCPServer`/`MCPClient`（tools/list + tools/call + to_harness_tools）+ `AgentEndpoint`/`a2a_tool` + `Skill`/`SkillLibrary`。
- 配套案例 `examples/16_protocol.py`：①MCP 原始调用 / ②MCP 接入 MiniAgent（位置透明）/ ③A2A 调专家（能力透明）/ ④Skills catalog+activate / ⑤三者对比——**全部无需 API**，确定性输出。
- 附录补齐：术语表补 16 内核组件与「MCP/A2A/Skills」；面试题库新增第 16 章 5 题带答法要点。
- 进度标识同步：侧边栏 16 转链接+阶段六去规划、README 徽章「01…16」、大纲 16 节加链接、第 15 章文末链向 16、FAQ/README 补 16 命令。

---

## 2026-08-26（十二）

### 🎉 发布第 15 章《终止条件 · 权限 · 安全》——阶段五 Runtime 层收官
- 正文：安全三件事 + 注入（终止/最小授权/审批/sandbox）、「走包装不走侵入」在工具层拦、三件套用法、黑名单 sandbox 与系统级隔离分层、串联 05/06/08/13/16。
- 内核第十三块砖：`harness/safety.py` 的 `StopConditions` + `ToolPolicy`/`PolicyGuard`（最小授权+审批+沙箱）+ `DenySandbox` + `detect_injection`/`InjectionReport`。
- 配套案例 `examples/15_safety.py`：①终止条件 / ②最小授权 / ③审批 / ④sandbox / ⑤注入检测 / ⑥组合进 MiniAgent（模型想删库被拦+回喂+改口）——**全部无需 API**，确定性输出。
- 附录补齐：术语表补 15 内核组件与「安全三件事/Prompt Injection」；面试题库新增第 15 章 5 题带答法要点。
- 进度标识同步：侧边栏 15 转链接+阶段五去规划、README 徽章「01…15」、大纲 15 节加链接、第 14 章文末链向 15、FAQ/README 补 15 命令。

---

## 2026-08-26（十一）

### 🎉 发布第 14 章《可观测：Trace / 日志 / 回放》——阶段五 Runtime 层
- 正文：log/metric/trace/replay 四件套分工、「不侵入 AgentLoop（包装 llm + 挂钩子）」、Tracer 四件套用法、ScriptedLLM 回放、接入 Langfuse 的 hook 定位。
- 内核第十二块砖：`harness/trace.py` 的 `TraceEvent` + `Trace` + `Tracer`（to_lines/metrics/timeline/llm_script）+ `ScriptedLLM`。
- 配套案例 `examples/14_trace.py`：①log NDJSON / ②metric / ③trace 时间线 / ④ScriptedLLM 离线回放（①–④ 无需 API）+ ⑤接 Langfuse 思路 + ⑥真实 LLM（需 API）。
- 附录补齐：术语表补 14 内核组件与「log/metric/trace/replay」；面试题库新增第 14 章 5 题带答法要点。
- 进度标识同步：侧边栏 14 转链接、README 徽章「01…14」、大纲 14 节加链接、第 13 章文末链向 14、FAQ/README 补 14 命令。

---

## 2026-08-26（十）

### 🎉 发布第 13 章《封装 Mini Agent Runtime》——阶段五 Runtime 层开局
- 正文：从「十块砖」到「一个 Agent」、Agent 状态机（new→running→done/error + 护栏 + reset）、事件循环 + 钩子（on_event 给 trace 铺路）、对比 LangGraph / OpenAI Agents SDK（价值观一致、量产细节不同）。
- 内核第十一块砖：`harness/runtime.py` 的 `AgentState` + `RuntimeEvent` + `MiniAgent`（委托 AgentLoop 干活、发 start/finish/error 事件、非法迁移报错）。
- 配套案例 `examples/13_runtime.py`：①状态机+reset / ②事件循环+钩子 / ③护栏→error / ④对比框架（①–③ 无需 API）+ ⑤真实 LLM（需 API）。
- 附录补齐：术语表补 13 内核组件与「Agent 状态机/事件循环」；面试题库新增第 13 章 5 题带答法要点。
- 进度标识同步：侧边栏 13 转链接+阶段五去规划、README 徽章「01…13」、大纲 13 节加链接、第 12 章文末链向 13、FAQ/README 补 13 命令。

---

## 2026-08-26（九）

### 🎉 发布第 12 章《Checkpoint 与状态恢复》——阶段四 State 层收官
- 正文：状态序列化（Plan↔dict）、断点续跑（只重跑没 done 的）、Durable Execution 三句话（状态是真源/SAVE 是常态/执行只推进状态）、at-least-once 与「失败即停」的取舍、串联 09/10/11/14。
- 内核第十块砖：`harness/state.py` 的 `plan_to_dict`/`plan_from_dict` + `save_checkpoint`/`load_checkpoint` + `run_plan_with_checkpoint`（每步前后落盘，failed 恢复时重跑）。
- 配套案例 `examples/12_checkpoint.py`：①序列化往返 / ②checkpoint.json 内容 / ③断点续跑（第 2 步崩溃→恢复→换 executor 做完）/ ④Durable Execution 思路——**全部无需 API**，确定性输出。
- 附录补齐：术语表补 12 内核组件与「Checkpoint/状态序列化/Durable Execution」；面试题库新增第 12 章 5 题带答法要点。
- 进度标识同步：侧边栏 12 转链接+阶段四去规划、README 徽章「01…12」、大纲 12 节加链接、第 11 章文末链向 12、FAQ/README 补 12 命令。

---

## 2026-08-26（八）

### 🎉 发布第 11 章《失败策略与反思》——Self-Reflection 与失败重试
- 正文：傻重试（06）vs 反思式重试（11）的边界、「失败策略不单独成范式、是给 05/10 兜底的配角」、Self-Reflection 本质（把失败回喂给模型）与第 07 章自纠的同一套路、串联 05/06/07/10/12。
- 内核第九块砖：`harness/reflection.py` 的 `reflect`（复用 structured_chat 输出 verdict/reason/next_action）+ `retry_with_reflection`（失败→反思→换思路→再试，超限/give_up 即停）+ `Reflection`/`ReflectionResult`。
- 配套案例 `examples/11_reflection.py`：①傻重试 vs 聪明重试 / ②reflect(FakeReflector) / ③retry_with_reflection 循环 / ④反思融入 Planning（①–④ 无需 API）+ ⑤真实 reflect（需 API）。
- 附录补齐：术语表补 11 内核组件与「Self-Reflection / 傻重试 vs 聪明重试」；面试题库新增第 11 章 5 题带答法要点。
- 进度标识同步：侧边栏 11 转链接、README 徽章「01…11」、大纲 11 节加链接、第 10 章文末链向 11、FAQ/README 补 11 命令。

---

## 2026-08-26（七）

### 🎉 发布第 10 章《任务拆解与 Planning》——阶段四 State 层开局
- 正文：ReAct 与 Plan-and-Execute 的本质区别与取舍（计划越好 P&E 越省、计划越可能错越回 ReAct）、「计划要成一等对象」的三个理由（可审查/可观察/可恢复）、串联 05/07/09/11/12。
- 内核第八块砖：`harness/planning.py` 的 `Plan`/`PlanStep`（步骤清单 + 状态机 pending→in_progress→done/failed）+ `make_plan`（复用第 07 章 structured_chat）+ `execute_plan`（逐步执行、失败即停）。
- 配套案例 `examples/10_planning.py`：①Plan 数据结构 / ②make_plan(FakePlanner) / ③execute_plan 失败即停 / ④ReAct vs P&E 对照（①–④ 无需 API）+ ⑤真实 make_plan（需 API）。
- 附录补齐：术语表补 10 内核组件与「ReAct / Plan-and-Execute」；面试题库新增第 10 章 5 题带答法要点。
- 进度标识同步：侧边栏 10 转链接+阶段四去规划、README 徽章「01…10」、大纲 10 节加链接、第 09 章文末链向 10、FAQ/README 补 10 命令。

---

## 2026-08-26（六）

### 🎉 发布第 09 章《记忆体系》——阶段三 Context 层收官
- 正文：记忆三层（短期/长期/向量，对应 ChatSession/FileMemory/VectorMemory）、「精确 vs 模糊」决胜（何时文件记忆、何时向量记忆）、RAG 与 Memory 的边界（读 vs 写）、向量库是检索加速器不是记忆本身。
- 内核第七块砖：`harness/memory.py` 的 `FileMemory`（key→value 落盘）+ `VectorMemory`（编码→余弦→top-k，embed 可注入）+ `toy_embed`/`cosine`（离线教具）+ `retrieved_context`（RAG 最小闭环）。
- 配套案例 `examples/09_memory.py`：①三层总览 / ②FileMemory 落盘跨会话 / ③VectorMemory 检索 / ④玩具嵌入的局限 / ⑤RAG 最小闭环——**全部无需 API Key**，确定性输出。
- 附录补齐：术语表补 09 内核组件与「RAG vs Memory」；面试题库新增第 09 章 5 题带答法要点。
- 进度标识同步：侧边栏 09 转链接、README 徽章「01·02·05·06·07·08·09」、大纲 09 节加链接、第 08 章文末链向 09、FAQ/README 补 09 命令。

---

## 2026-08-26（五）

### 🎉 发布第 08 章《Context 治理》——阶段三 Context 层开局
- 正文：第 02 章埋的三根刺（越聊越大/越聊越贵/越聊越失焦·lost in the middle）、三条治理路线与决策表（压缩/取舍/按需读取 +「热而短压进去、大而冷存引用」）、决策框架三问。
- 内核第六块砖：`harness/context.py` 的 `estimate_tokens` + `keep_last_turns` + `summarize`/`compact` + `select_by_budget` + `ReferenceLibrary`——全是「messages → messages」纯函数，不改任何已有签名。
- 还第 02 章一笔债：`ChatSession.truncate` 加固为「按轮成对裁」，奇数 N 不再让历史以 assistant 开头（偶数 N 行为不变）；新增 `keep_last_turns` 作为按轮原语。
- 配套案例 `examples/08_context_governance.py`：①估算+按轮成对裁 / ②按预算取舍 / ③compact(FakeLLM) / ④ReferenceLibrary 按需读取 四个纯治理层 demo **无需 API Key**，⑤真实 LLM 压缩（需 API）。
- 附录补齐：术语表补 08 内核组件与「lost in the middle」；面试题库新增第 08 章 5 题带答法要点。
- 进度标识同步：侧边栏 08 转链接+阶段三去规划、README 徽章「01·02·05·06·07·08」、大纲 08 节加链接、第 07 章文末链向 08、FAQ/README 补 08 命令。

---

## 2026-08-26（四）

### 🎉 发布第 07 章《结构化输出与 Schema 设计》——主线五连收官
- 正文：为什么模型输出需要结构、三层武器（JSON Mode → Pydantic 校验 → 自纠重试）、schema「一鱼两吃」（约束工具入参 vs 模型输出）、JSON Mode 与原生 structured output 的取舍。
- 内核第五块砖：`harness/schema.py` 的 `StructuredOutputError` + `extract_json` + `strict_validate` + `structured_chat`（Pydantic 校验 + 解析失败回喂自纠），**`LLMClient.chat` 签名不动**——结构化输出是 chat 之上的包装。
- 配套案例 `examples/07_structured_output.py`：①extract_json 容错 / ②Pydantic 校验三类失败 / ③自纠重试（FakeLLM 确定性演示）三个纯解析层 demo **无需 API Key**，④真实 LLM 情绪分析（需 API）。
- 附录补齐：术语表补第 06、07 章内核组件与「幂等/自纠重试/结构化输出」术语；面试题库新增第 06 章（容错层）与第 07 章（结构化输出层）各 5 题带答法要点。
- 进度标识同步：侧边栏 07 转链接、README 徽章「01·02·05·06·07」、大纲 07 节加链接、第 06 章文末链向 07。

---

## 2026-08-26（三）

### 🎉 发布第 06 章《工具集合与容错》——三部曲之后的第一块器官
- 正文：为什么工具边界要容错（模型重试 vs 工具重试的成本）、重试/超时/幂等三件套、工具失败的三种兜底（回喂模型 / 工具重试 / 降级）、「重试只在幂等前提下」红线。
- 内核第四块砖：`harness/tools.py` 的 `ToolError` + `RetryPolicy` + `ResilientTool` + `ToolRegistry`（重试 / 超时 / 幂等），**`AgentLoop.run()` 签名零改动**——容错做在工具层，`ResilientTool` 与 `Tool` 同接口被循环无感调用。
- 配套案例 `examples/06_tool_retry.py`：①重试 / ②超时 / ③幂等（非幂等拒重试 + 幂等键去重）三个纯工具层 demo **无需 API Key**，④循环里重试 vs 不重试对比（需 API）。
- 进度标识同步：侧边栏 06 转链接、README 徽章「01·02·05·06」、大纲 06 节加链接、第 05 章文末链向 06。

---

## 2026-08-26

### 🔧 三部曲发布后的对齐修正（按修改建议）
- 第 02 章预期输出与脚本对齐：demo①「6 条」→「7 条（1 system + 3×2）」、demo③「截断后 3 条」→「2 条」、§2.3「两轮累积」→「三轮累积」。
- 进度/命名漏网：大纲顶部进度更新为「已发布 01、02、05」；`examples/__init__.py` 清掉不存在的 `05_react.py`，列出现有三个案例；第 01 章案例 `check_env` 改读内核 `PROVIDERS`（与 02/05 一致）。
- 三个案例的 `check_env` 统一加未知 `LLM_PROVIDER` 的友好报错（原为裸 `KeyError`）。
- 第 05 章教学风险点补两处：§3.1 点破「为什么不复用 ChatSession」（会话态 vs 任务态，第 13 章再合）；§5 加「模型可能不调工具，输出不一致先别怀疑环境」提示（FAQ 同步）。
- FAQ 第 4 节补 02/05 运行命令；README 快速开始加「跑通后可继续」一句；CONTRIBUTING 修正「必须根目录跑」措辞，补「案例自检读 PROVIDERS」规则。
- 附录增量：面试题库〇节扩为三章结构（01 调用层 / 02 状态层 / 05 循环层，各 5 题带答法要点）；术语表新增「本教程内核组件」分组（LLMClient / ChatSession / Tool / AgentLoop / tool 角色 / max_rounds / stopped_by，均带首次出现章节）。

---

## 2026-08-24（三）

### 🎉 发布第 05 章《手写第一个工具循环》——「调用 → 状态 → 循环」三部曲收官
- 正文：Function Calling 最小闭环（schema / tool_calls / role="tool"）、手写 AgentLoop、终止条件与护栏（自然终止 vs 预算终止）、「循环是本体，ReAct 是教具」的定位。
- 内核第三块砖：`harness/loop.py` 的 `Tool` + `AgentLoop`（工具包装 / 循环 / max_rounds 护栏 / 工具异常回喂），`run()` 签名从本章冻住。
- 还代码债（上轮建议 §2.4）：`LLMResult.tool_calls` 从 SDK 对象收成 `list[dict]`，上层（AgentLoop）全程不 import SDK。
- 配套案例 `examples/05_tool_loop.py`：单轮工具调用、多轮链式（先查时间再算账）、护栏硬停三个 demo，并打印执行轨迹（消息列表角色流）。
- 进度标识同步：侧边栏 05 转链接、README 徽章「已发布 01·02·05」、大纲 05 节加链接、第 02 章文末链向 05。

---

## 2026-08-24（二）

### 🎉 发布第 02 章《消息状态与上下文窗口》
- 正文：无状态 API 本质（消息列表 = Agent 第一个状态）、Token 滚雪球账单、上下文窗口与 KV Cache（前缀稳定守则）、长上下文三个坑、历史治理第一刀（截断）。
- 内核第二块砖：`harness/session.py` 的 `ChatSession`（攒历史 / 累积用量 / 截断），`ask()` 签名从本章冻住。
- 配套案例 `examples/02_chat_history.py`：多轮记忆、每轮 token 增量、截断前后对比三个 demo；案例的环境自检直接读内核 `PROVIDERS`，不再重复维护提供商表。
- 进度标识同步：侧边栏 02 转为可点链接，README 徽章改「已发布 01–02」，大纲 02 节加链接。

### 🔧 章名全局对齐
- 侧边栏「05 ReAct 循环 → 05 工具循环」「11 反思与自纠 → 11 失败策略与反思」「09 记忆体系：文件 vs 向量」，与 README / 大纲一致。
- 第 01 章导语、阶梯图、5.1/5.2 提示、5.3 意义段清除 ReAct/RAG 旧表述，统一为「工具循环 / 消息状态 / 记忆」新主线。

---

## 2026-08-24（一）

### ✅ 已发布第 01 章
- 第 01 章《LLM 调用与环境准备》：调用三件套、同步/流式、封装 `LLMClient`。
- 配套案例 `examples/01_hello_llm.py`。

### 🔧 完成度诚实化（按修改建议 P0–P3）
- 侧边栏：未发布章节标「规划中」，不再做成可点链接（避免 404）。
- README 加真实进度徽章与「已发布 01」说明。
- 代码重构为会生长的内核：`harness/`（内核）+ `examples/`（案例），案例用「相对仓库根的 `sys.path`」import 内核（内核本身不靠它），配置改用 `find_dotenv`；`LLMClient` 对外返回 `LLMResult`，接口从第 01 章起冻住。
- 第 01 章页眉加章节导航；代码链接标「可本地运行」。
- `.env-example` 补齐 OpenAI 两项；依赖拆为 `requirements.txt`（轻）+ `requirements-full.txt`（重）。
- docsify `index.html` 补 repo 地址；GitHub Pages 发布范围缩小到站点文件。
- LICENSE 版权行改作者名。

### 🔧 文档与代码对齐（2026-08-24）
- 清掉「旧世界」残留：FAQ 第 4 节、第 01 章 3.4 / 第 7 节不再写「必须在根目录跑」，改为 `find_dotenv` 任意子目录可读 `.env`；第 01 章文末「下一章」死链改为纯文本「规划中」。
- `教程目录大纲.md` 02–18 章去掉可点链接，只列知识点，避免 404。
- CONTRIBUTING 的 `sys.path` 策略与案例代码对齐（方案 A：案例允许相对仓库根的 `sys.path`，内核不靠它）。
- README 首页卖点对齐「从 Prompt 到 Runtime」主线：上下文治理 / 工具失败 / checkpoint / trace / 权限为主，向量库 / 反思降为教具。

---

## 2026-08-23

### 🎉 仓库初始化
- 确立教程主线：**从 Prompt 到 Runtime**，聚焦「从零手写 Agent Harness」。
- 完成仓库骨架：README、教程目录大纲、6 阶段 18 章规划、附录占位。
- 配套 docsify 在线阅读站（`index.html` + `_sidebar.md`）。
- 配置文件就绪：`.gitignore`、`.env-example`、`requirements.txt`、`LICENSE`。
