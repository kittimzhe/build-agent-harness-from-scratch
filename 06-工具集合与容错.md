# 06 - 工具集合与容错

> 📌 **第 6 章 · 阶段二 Tool 层** · [← 返回目录大纲](教程目录大纲.md) · [上一章 05 手写第一个工具循环 →](05-手写第一个工具循环.md) · 下一章 07 结构化输出与 Schema 设计（规划中）

---

**本章课程目标：**

- 看清一个事实：**真实世界的工具会失败、会卡死、会有副作用**。第 05 章「把错误回喂模型」只是兜底，不是方案——模型重试一轮的成本（重发全部历史 + 重新生成）高得离谱。
- 掌握容错三件套：**重试（retry）/ 超时（timeout）/ 幂等（idempotency）**，以及它们各自的边界和陷阱。
- 把容错做在「工具层」而不是「循环层」：`ResilientTool` 包在 `Tool` 外面，`AgentLoop.run()` 一个字节都不用改——再次印证「内核只加能力、不改签名」的纪律。
- 落地第四块砖：`harness/tools.py`（`RetryPolicy` / `ResilientTool` / `ToolRegistry`）。

**学习建议：** 本章 demo ①–③ **不需要 API Key**，直接跑；④ 走循环才需要 `.env`。先把 ①–③ 的确定性输出验证清楚（它们在教你判断「什么时候能重试」），再跑 ④ 感受省下的轮数。

**官方文档与资源**：[云厂商超时与重试最佳实践](https://learn.microsoft.com/zh-cn/azure/architecture/best-practices/transient-faults) · 幂等性设计（HTTP 语义）· 更多见 [工具导航](工具导航与参考资料索引.md)。

---

## 1、为什么工具边界要容错

第 05 章的工具循环，遇到工具失败时是这么处理的：捕获异常 → 把错误文本作为 `role="tool"` 回喂 → 模型看到「工具执行出错：……」→ 自己决定重试、换参数还是放弃。

**这能用，但它把重试的成本外包给了模型。** 模型每多走一轮，代价是：

1. **重发全部历史**：第 02 章讲过，每轮 prompt_tokens ≈ 之前所有轮的总和；
2. **重新生成**：又要支付一次 completion 的 token 和延迟；
3. **不确定性**：模型可能不重试、改错方向、甚至直接瞎编一个结果。

而很多失败**根本不需要模型出面**——比如「服务瞬时抖了一下」，你在工具内部重试 1 次，0.5 秒就能解决；让模型重试，要多烧几百 token、多等好几秒。

> **工程判断一句话**：瞬时故障在工具边界内吞掉；真正需要「智能决策」的失败才回喂给模型。前者便宜，后者贵。

| 本教程 | 框架里对应的东西 |
| --- | --- |
| `RetryPolicy` | Agents SDK 不自带重试（要自己包）；LangChain `tool` + `retry`；工程上常配 `tenacity` / `httpx` 超时 |
| `ResilientTool` | 带重试/超时的 function tool 包装 |
| `ToolRegistry` | 集中管理工具集合与策略（MCP 的 tool 集合也是这个思路） |

---

## 2、三件套：重试、超时、幂等

### 2.1 重试（retry）

瞬时失败（网络抖动、限流、临时锁）重试是最划算的。要点：

- **指数退避**：0.5s → 1s → 2s …… 而不是固定间隔硬撞（防止重试风暴）。
- **只重试「可重试」的错误**：入参错、权限错重试一万遍也没用，还给服务加压。
- **有上限**：`max_retries`，宁可失败也别无限试。

### 2.2 超时（timeout）

工具是外部依赖（HTTP、数据库、shell），**可能挂起而不是报错**。没有超时，你的 Agent 就卡在第 2 轮永远不回来。要点：

- 每个工具给一个合理预算（查询类可以短、复杂任务可以长）；
- 超时≠取消：Python 里 `threading` 没法真正杀线程，超时只是「不再等」——生产环境要配合取消/异步。

### 2.3 幂等（idempotency）

**这是三件套里最容易被忽视、也最容易出事故的一个。** 幂等 = 同一个操作执行多次，结果和执行一次一样（`add(1,2)` 幂等；`下单()` 不幂等）。

- **重试的前提是幂等**：对非幂等工具盲目重试 = 重复下单 / 重复扣款。
- 非幂等工具要想安全重试，要靠**幂等键（idempotency key）**：调用方带上唯一 key，服务端用 key 去重——同 key 第二次调用直接返回第一次的结果，而不是再执行一次。

> 本章内核把这件事显式化了：`RetryPolicy.idempotent=False` 时，即使配了 `max_retries`，容错层也**拒绝自动重试**，把决定权交出来，而不是替你莽撞地重复副作用。

---

## 3、内核：harness/tools.py（第四块砖）

三个新组件，全部独立于 `AgentLoop`：

### 3.1 RetryPolicy + run_with_policy

策略和数据类，配置重试/超时/幂等；`run_with_policy()` 是执行引擎（核心就在这一个 while 循环）：

```python
def run_with_policy(func, args, name, policy=None):
    policy = policy or RetryPolicy()
    attempt = 0
    delay = policy.base_delay
    while True:
        try:
            return str(_call_with_timeout(func, args, name, policy.timeout))
        except Exception as e:
            # 关键门槛：只有幂等工具才自动重试
            if attempt < policy.max_retries and policy.idempotent:
                attempt += 1
                time.sleep(delay)
                delay *= policy.delay_multiplier
                continue
            note = "（已重试 %d 次）" % attempt
            if policy.max_retries > 0 and not policy.idempotent:
                note = "；该工具标记为非幂等，未自动重试（避免重复副作用）"
            raise ToolError(f"工具 {name!r} 执行失败{note}：{e}") from e
```

阅读顺序：**能超时就先超时；失败后先过「幂等」这道门槛，再决定重试；重试尽了，统一抛 `ToolError`，把干净的失败信息交回上层**。

### 3.2 ResilientTool

包在 `Tool` 外面、相同接口：

```python
class ResilientTool(Tool):
    def __init__(self, func, ..., policy=None):
        super().__init__(func, ...)
        self.policy = policy or RetryPolicy()

    def run(self, **kwargs):
        return run_with_policy(self.func, kwargs, self.name, self.policy)
```

因为 `run()` 签名和 `Tool` 一致，**`AgentLoop` 分不清自己拿的是 `Tool` 还是 `ResilientTool`**——这就是「加能力不改签名」能成立的底层原因：多态 + 协议一致。

### 3.3 ToolRegistry

工具多了之后要集中管理：注册、查 schema、按各自策略执行、幂等键去重。

```python
registry = ToolRegistry()
registry.register(ResilientTool(slow_lookup, policy=RetryPolicy(timeout=1.0)))
registry.run("slow_lookup", {"keyword": "x"})                     # 走超时策略
registry.run("create_order", {"item": "咖啡"}, idempotency_key=k)  # 同 key 去重
```

完整实现见 [harness/tools.py](harness/tools.py)。

> 🧱 第四块砖落位：`LLMClient`（调用）+ `ChatSession`（状态）+ `AgentLoop`（循环）+ `tools`（容错）。到此，你的 Agent「骨架有了、手脚有了、手脚还不会把自己绊倒」——下一层的能力（schema 设计、上下文治理、状态恢复）都是往这副骨架上继续加。

---

## 4、工具失败的三种兜底（串联 05 与 06）

把第 05、06 章串起来，工具失败有且只有三层应对，从贵到便宜：

| 层级 | 策略 | 成本 | 适用 |
| --- | --- | --- | --- |
| 1 | **回喂模型**（05 已做） | 高（重发历史 + 重新生成） | 需要「智能判断」的失败：结果语义不对、需要换策略 |
| 2 | **工具边界重试**（06 本章） | 低（只重发一次工具调用） | 瞬时抖动、超时，且工具幂等 |
| 3 | **降级 / 兜底默认值** | 最低（可能 0 次模型调用） | 工具不可用但任务有可接受的默认答案 |

第 3 层本章只示范一下「降级」的思路，不做进内核（你按业务需要自己包一层 try/except 即可）：

```python
try:
    result = resilient_tool.run(query=q)
except ToolError:
    result = "（实时数据不可用，返回缓存于昨日的快照）"   # 降级，而不是整体失败
```

> 面试常问「工具调用失败了怎么办」，能分这三层讲、并说清「重试只在幂等前提下」的人，比只会说「给模型看错误」的人高一个段位。

---

## 5、运行本章案例

demo ①–③ 纯工具层，**无需 `.env`**；demo ④ 走循环才需要（配置见 [第 01 章环境准备](01-LLM调用与环境准备.md#3、环境准备)）。

```bash
python examples/06_tool_retry.py
```

预期输出（①–③ 是确定性的）：

```
① 重试：瞬时抖动重试 3 次后成功
调用 tool.run(a=1, b=2) → 3
工具内部实际执行了 4 次（前 3 次失败自动重试，第 4 次成功）

② 超时：1 秒预算拦住 2 秒的慢工具
捕获 ToolError：工具 'slow_lookup' 超时（超过 1.0s）。原线程可能仍在后台运行…

③ 幂等：非幂等工具拒重试 + 幂等键去重
(a) 非幂等工具第一次失败后：工具 'charge' 执行失败；该工具标记为非幂等，未自动重试（避免重复副作用）
(b) 同幂等键两次下单：
    第 1 次 → 已下单：咖啡（第 1 单）
    第 2 次 → 已下单：咖啡（第 1 单）
    OrderCreator 实际执行了 1 次（去重生效，没有二次扣款）

④ 循环里的重试：ResilientTool 让模型不看见瞬时故障
--- 对照组：普通 Tool（无重试）---
轮数：3，工具实际执行：2 次
--- 实验组：ResilientTool（重试 1 次，幂等）---
轮数：2，工具实际执行：2 次
💡 对照组 3 轮 vs 实验组 2 轮——每省 1 轮 = 少重发全部历史 + 少一次生成
```

> ⚠️ demo ④ 依赖模型行为，输出可能和上面不完全一致（和第 05 章同一类非确定性）。要点是看**对照组的轮数 ≥ 实验组**：只要模型第一轮调了工具，重试那轮就从「模型的重试」变成了「工具的重试」，消耗更低。数字不一别慌，方向一致就对。另外实验组「工具执行 2 次」是正常的——容错层就是替模型多执行了一次工具，但这 2 次执行不产生新的「模型生成」成本。

---

## 6、常见报错排查

| 报错 / 现象 | 原因 | 解决 |
| --- | --- | --- |
| 配了 `max_retries` 却不重试 | `idempotent` 没设为 `True` | 这是故意门槛：先确认工具幂等，再开重试 |
| 超时后 CPU 还高/结果还在写 | 超时只是「不再等」，没杀线程 | 生产环境用异步/取消机制；本章 `ThreadPoolExecutor` 只是教学实现 |
| 幂等键去重「没生效」 | 两次调用用了不同的 key | key 是调用方生成并保证唯一，通常来自请求 ID / 订单号 |
| 重试把服务打挂 | 固定短间隔硬撞 | 用指数退避 + 抖动（jitter），`delay_multiplier` 已示范 |
| `AgentLoop` 里拿不到重试 | 传的是 `Tool` 不是 `ResilientTool` | 用 `ResilientTool` 包一下即可，循环代码不用动 |

更多见 [新手入门与常见问题](新手入门与常见问题.md)。

---

## 7、本章小结与下一章

✅ 你现在已经能：

- 说清「为什么要在工具边界容错」：模型重试比工具重试贵得多
- 用 `RetryPolicy` 配置重试 / 超时 / 幂等，讲清三者的边界
- 用 `ResilientTool` 无侵入地给 `AgentLoop` 加容错，`run()` 签名不动
- 用 `ToolRegistry` 集中治理工具集合 + 幂等键去重
- 分三层讲工具失败（回喂模型 / 工具重试 / 降级），并守住「重试只在幂等前提下」的红线

➡️ 下一章 [**07 结构化输出与 Schema 设计 →**](07-结构化输出与Schema设计.md)：让模型的输出**可判别、可解析、禁得起格式漂移**——工具能「说清自己要什么参数」，模型才能「给对参数」，这是容错之后的下一个可靠性课题。可回[教程目录大纲](教程目录大纲.md)看全局。